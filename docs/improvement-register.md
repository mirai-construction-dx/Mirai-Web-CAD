# 改善台帳

更新日: 2026-09-18(前回 2026-09-05)。工数は1人日=実働8時間の概算であり、調達・契約・UAT待ちは含めない。

**2026-09-18ラウンド要旨**: 総合評価(業務適合性・機能完成度・UI/UX・アクセシビリティ・データ品質・AI有効性・設計・コード品質・性能拡張性・セキュリティ・可用性・監視・テスト・CI/CD・運用保守性・文書・費用対効果・競合代替性)を6件の並列監査で証拠ベース実施。最重要の発見は**GET /api/drawings/:idを含む全drawing系エンドポイントに案件単位のアクセス制御が存在せず、認証済みなら任意ロールが任意図面を取得できるIDOR相当の欠落**(3監査が独立に重大度「最優先/今すぐ」で指摘)。P1-01(案件/組織/図面ACL)の一部として即日実装・テスト・本番デプロイまで完了(PR #95)。CI依存関係監査ゲート追加(PR #96)、`sharp`/`wrangler`の既知high脆弱性2件をDependabot経由でマージ解消(PR #91)。**新たに発覚した重大な運用ギャップ**: 本番ホストのローカルmainが、CodeRabbit指摘14件(実バグ1件含む)が未解決のままレビュー未完了のfeat/native-dimension-hatch-viewport(PR #87、DIMENSION/HATCH/VIEWPORT対応)を、GitHub main反映前に直接稼働させていた。ブランチ保護の`required_conversation_resolution`によりPR #87は正規手順でマージ不能なため、今回は本番ホストのローカルのみでorigin/mainとマージ統合し、レビュー未完了機能を維持したままセキュリティ修正を反映した(次回のPR #87正式マージで解消される暫定措置)。詳細は[本番運用適合性評価書](production-readiness-assessment.md)2026-09-18ラウンドを参照。

**優先順位の最上位参照文書**: [80～90％代替・AI統合開発方針](Mirai-Web-CAD_80-90％代替・AI統合開発方針.md)(2026-08-30、ユーザーがCTOへ対応・開発・実装を全権委任)。現状代替率30-35%、土木2Dワークフロー限定で80-90%到達を目標とするPhase 0-5ロードマップを定義。DXF往復・精密CAD Core・尺度保証PDFを最優先とし、AI機能拡張より先にCAD基盤の完成を優先する方針。本台帳のP1-03(DXF書出し)・P2-01(性能)・P1-06(レイアウト/PDF)等は、この方針のPhase 1(80%到達の必須範囲)に対応する。DWG対応は[ADR-0002](adr/ADR-0002-dwg-scope-drop-dxf-only.md)により恒久的に対象外へ変更された(2026-08-30)。

## 今すぐ

継続追加: [高度な選択・追加編集](advanced-selection-editing.md)の範囲でFence/Lasso、Previous/Last、類似/条件選択、保存セット、LENGTHEN/REVERSE/PURGE/OVERKILLを実装。全形式対応、許容差重複統合、100k性能・実案件受入は後続。

2026-09-05追加依頼: [追加実装ロードマップ](additional-implementation-roadmap.md)へ34領域の依存・実装順・受入条件を整理。複数選択/Window/Crossing/グリップ、STRETCH/EXPLODE/MATCHPROPの限定実装を開始。全領域や実案件互換認定の完了とは区別する。

同日継続実装: [連想寸法](associative-dimensions.md)を追加。線分のAligned/水平/垂直、円/円弧の半径/直径、参照再計算・参照切れ検査・寸法書式・オフセットグリップ・JSON参照ID再割当を実装。角度/累進/公差/名前付き寸法スタイル/DXF寸法往復/PDFは未完了。P0-48等の残タスク記載は各項目の実装時点の履歴であり、現行範囲は追加ロードマップを参照。

| ID | 内容 | 理由/対象 | 効果 | 難易度・工数 | 優先度 | 依存/リスク | 完了基準 | 状態 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P0-01 | 匿名閲覧と更新認証を分離 | 全利用者/権限逸脱防止 | 公開性と機密性を両立 | 中 3日 | P0 | Access設計 | public図面のみ200、全writeはfail-closed(2026-08-30〜Cloudflare Access保護によりエッジ層302、アプリ層到達時401) | 完了 |
| P0-02 | DB更新の原子化 | CAD担当/データ不整合防止 | 図面・版・監査・冪等性を同時確定 | 高 4日 | P0 | Neon SQL | 障害時に部分更新なし | 完了 |
| P0-03 | CSP等の配信header | 全利用者/XSS・clickjack低減 | browser防御 | 低 1日 | P0 | CSP回帰 | Header実測、E2E成功 | 完了 |
| P0-04 | JSON 1 MiB/Content-Type/CORS制限 | 運用/濫用低減 | API資源保護 | 低 1日 | P0 | 大容量Import方式 | 413/415/CORS test | 完了 |
| P0-05 | backup/restore drill | 運用/消失対策 | 手順の機械検証 | 中 2日 | P0 | PG client | 空DB復元と件数検証 | 完了 |
| P0-06 | Entra ID + Cloudflare Access再構成 | 社員/永続編集 | SSO/MFA/RBAC | 中 3-5日 | P0 | IdP管理者、HENNGE方針 | 社員3roleでE2E | 一部完了: 2026-08-30にCloudflare Access Application(`mirai-web-cad-api`、`/api/*`保護、当時はOne-Time PIN、`kensan1969@gmail.com`のみallow)を新設し、管理者本人による書込み動作を確認。2026-09-05現在のMVPログインはCloudflareアカウント認証であり、古いOTP表記と区別。Entra ID/HENNGE ONE連携、複数利用者・roleマッピングの本格運用は未着手 |
| P0-07 | ~~Neon main保護・復旧窓延長~~ | ~~運用/誤削除防止~~ | ~~RPO改善~~ | — | — | — | — | **失効(2026-08-30)**: 2026-08-30にNeon PostgreSQL依存を完全に除去しローカルPostgreSQL + Cloudflare Tunnelへ移行したため、Neon側の保護設定は対象外になった。RPO/RTOはP0-12(ローカルバックアップ自動化)で別途管理する |
| P0-08 | Production環境承認Gate | CTO/誤deploy防止 | 変更統制 | 低 1日 | P0 | GitHub権限 | reviewer必須、rollback確認 | 一部完了: branch protection必須check4件。required reviewerはプラン制約(Issue #9) |
| P0-09 | 合成監視・通知 | IT/DX 7名 | 発見時間短縮 | 中 2日 | P0 | 通知先 | 5分監視、health/demo/write境界alert | 一部完了: healthにstatus/version/timestamp/503を実装(2026-08-27)。GitHub Actions 15分間隔の合成監視+`incident`+`synthetic-monitor`両ラベルIssue自動起票+復旧時自動close、Webhook通知(`MONITOR_WEBHOOK_URL`任意設定)を実装(2026-08-29、CodeRabbitレビュー対応で誤close防止の専用ラベルを追加)。重大度定義・SLA目標・エスカレーション経路・当番表テンプレートを`docs/operations.md`に追加(2026-08-30)。schedule実行間隔はGitHub仕様上の保証なし、**当番の実名割当(人手入力待ち)**とCloudflare/Neon内部5xx相関は未着手 |
| P0-10 | 監査ログのDB追記専用化 | 監査者/改ざん防止 | 権限保有者でも改変不可 | 低 0.5日 | P0 | 0005 migration | トリガー2件、UPDATE/DELETE拒否をCI・本番で検証 | 完了 (2026-08-27) |
| P0-11 | 監査ログCSV export・ページング | 監査者/説明責任 | 監査データの棚卸が可能 | 低 1日 | P1 | 数式注入対策 | `POST /api/audit-logs/export`(`content-type: application/json`必須)が承認権限のみ、export自体を監査(2026-09-18に`GET ?format=csv`から変更。GETで状態を変更しないため) | 完了 (2026-08-27、2026-09-18に経路変更) |
| P0-12 | 本番バックアップ自動化 | 運用/RPO改善 | 日次archiveの機械実行 | 低 1日 | P0 | ローカルPostgreSQLへの移行(P0-19) | 日次バックアップ成功、鮮度検証成功 | **完了(2026-08-30)**: Neon依存の除去に伴いGitHub Actions版(`backup-production.yml`)を廃止し、`mirai-web-cad-backup.timer`(systemd、日次03:10 JST、読み取り専用ロール使用)+`mirai-web-cad-backup-check.timer`(鮮度検証06:00 JST)へ全面移行。実機で動作確認済み |
| P0-19 | Neon PostgreSQL依存を完全除去 | 全利用者/ユーザー指示への対応 | ローカルPostgreSQL + Cloudflare Tunnelへ本番永続化を移行 | 高 1日 | P0 | ローカルPostgreSQL稼働、Cloudflare Tunnel権限 | **現行の本番経路(`scripts/serve-production.mjs`→ローカルPostgreSQL)にNeon依存が無いこと**。本番`/api/health`が`provider=postgres, mode=connected`を返すことで確認する(ロールバック用に残置しているCloudflare Pages `functions/api/`側のコードはこの基準の対象外。Phase 7でPages自体を削除する際に合わせて整理する) | **完了(2026-08-30)**: `src/data-store.js`を`postgres`(postgres.js)へ書き換え、`scripts/serve-production.mjs`新規、systemd化、Cloudflare Tunnel作成、DNS切替まで実施。本番実測でSPA/health/demo 200、write 401を確認。Issue #22はこれにより解決としてclose |
| P0-13 | CORS複数オリジン対応 | 運用/Pages既定URLとCustom Domain両立 | 許可リスト外オリジンは反映しない安全な多origin対応 | 低 0.5日 | P1 | 既存単一origin利用者への影響確認 | 許可リスト内originのみ反映、リスト外は既定originを返すテストで確認 | 完了 (2026-08-29) |
| P0-14 | API未接続時のfail-open是正 | 承認者/整合性 | 検査OK誤表示・ロール自己切替・ローカル完結承認を排除 | 中 1日 | P0 | 外部評価アドバイスの指摘(P0 #2〜#4) | roleLocked fail-safe化、検査不能表示、レビュー/承認/新版ボタンをAPI接続確認済みかつ権限ありのみ活性化 | 完了 (2026-08-29) |
| P0-15 | レイアウト崩壊・CSP不整合の修正 | CAD担当/表示破綻防止 | inline styleのCSPブロックによるcomputed width/height=0を解消 | 中 1日 | P0 | 外部評価アドバイスの指摘(P0 #5) | CSSOM経由の動的スタイル設定へ変更。serve-local.mjsに`_headers`適用を追加しローカル/E2Eで回帰検出可能に | 完了 (2026-08-29) |
| P0-16 | README/トレーサビリティの実態同期 | 全利用者/文書正本の信頼性 | 「実装済み」一辺倒表記を試作/限定対応/実案件認定済みの3段階へ統一 | 低 0.5日 | P0 | 外部評価アドバイスの指摘(P0 #7) | 機能表に段階列と実装限界の注記を追加 | 完了 (2026-08-29) |
| P0-17 | GitHub Advanced Security機能有効化 | セキュリティ/供給網統制 | vulnerability alerts, secret scanning, push protection, private vulnerability reporting, dependabot security updatesを有効化 | 低 0.1日 | P0 | Publicリポジトリのため無償 | `security_and_analysis`全項目enabled、vulnerability-alerts/private-vulnerability-reporting enabled | 完了 (2026-08-29) |
| P0-18 | state.json整備 | 運用/セッション間の状態継承 | goal・blocked_issues・learningを機械可読で記録 | 低 0.2日 | P1 | 外部評価アドバイスの指摘(P0 #8) | state.json新規作成、Issue #22等のblockerを記録 | 完了 (2026-08-29)。専用GitHub Projects(v2)の整備は未着手 |
| P0-21 | CI `Deploy Preview`ジョブの`Verify preview`が恒常的に失敗 | 運用/CI可読性 | PR毎に赤いジョブが残り、真の異常との判別を妨げる | 低 0.5日 | P2 | Pages Functions側のNeon接続コード | PR CIで`Deploy Preview`がgreenになる、またはNeon依存の実態に合わせてチェック内容を見直す | **完了(2026-09-05、PR #66)**: 正規APIはCloudflare Tunnel + ローカルPostgreSQLへ移行済みで、Pagesは静的SPAのPreview/ロールバック用途に限定した。`Verify preview`から旧Neon依存の`/api/health=200`判定を外し、SPA 200だけを検証するよう更新。API/DBは通常CIの実PostgreSQL統合と、指定MVP URLの公開E2Eで検証する |
| P0-22 | 閲覧者ロールのUIボタン活性化制御 | 全利用者/権限UXの明確化 | 80-90%方針§12直近アクション#1「閲覧者でも新規、保存、作図、移動、削除等のボタンが有効」の是正、80-90%方針§6.5「権限不足の理由を表示する」 | 低 1日 | P0 | ROLE_POLICIES.canEdit/canApprove | 閲覧者ロールで新規図面/保存/開く/作図・修正・注釈系リボンボタンがdisabledかつtitle/aria-labelに理由表示、選択・計測・表示系は引き続き有効 | 完了(2026-08-30、CodeRabbitレビュー対応込み): `src/app.js`の`ribbonButtonHtml`にtool/act種別からcanEdit/canApprove要否を判定する`ribbonButtonDisabled`を追加。`newDrawingBtn`/`importBtn`/`quickSaveBtn`にも同条件を適用し、disabled時はtitle/aria-labelへ「（〇〇は利用できません）」を付加。サーバー側のfail-closed(P0-14)は既存のまま無変更、UI層の追加防御。E2E回帰テストを更新(disabled状態の検証) |
| P0-23 | A4/A3レイアウトプレビューの未保存不整合 | CAD担当/レイアウト作業の信頼性 | 80-90%方針§12直近アクション#3「A4選択とA3横プレビューが同時に表示される場合がある」の是正 | 低 1日 | P0 | `#layoutForm`のinput/change、`state.layoutDraft` | 用紙サイズ変更が「設定保存」を押す前でもプレビュー(サイズ・ラベル)へ即時反映され、印刷・空間切替・タブ切替後も未保存値が保持される | 完了(2026-08-30、CodeRabbitレビュー対応込み): `previewLayoutFromForm`が`state.layoutDraft`へ未保存フォーム値を保持し、`activeLayoutDrawing()`経由で`layoutSpaceHtml`/`applyLayoutGeometry`/`printDrawing`が同じdraft値を参照するよう修正(当初はDOMのみの一時更新で、印刷・タブ切替時に保存済み値へ戻る不具合があった)。「設定保存」成功時・新規図面作成時にdraftをクリア。回帰E2Eテスト追加(A4/A3/A1切替、空間切替後の保持) |
| P0-24 | `deploy-local.sh`のhealth check grepが常時不一致 | 運用/本番反映の信頼性 | 80-90%方針§12直近アクション#7「最新mainの承認型本番反映」作業中に発見。正常デプロイでも毎回自動ロールバックしていた | 低 0.1日 | P0 | なし | health checkのgrepが整形済みJSONにマッチする | 完了(2026-08-30、PR #35): `grep -q '"ok":true'`(スペースなし厳密一致)を`grep -qE '"ok":[[:space:]]*true'`へ修正 |
| P0-25 | 保存正本(サーバ/ブラウザ一時/未同期/競合)の統一表示 | 全利用者/正本の明確化 | 80-90%方針§12直近アクション#4「サーバ正本、ブラウザ一時保存、未同期、同期済み、競合、オフラインを常時表示すべき」への対応 | 中 2日 | P0 | 保存状態stateの新設設計 | 単一の状態指標(saved-local/synced/unsynced/conflict/offline等)をヘッダーまたは設定に常時表示 | 完了(2026-08-30): `saveStatusInfo()`を新設し、`state.apiStatus`(接続状態)と新規`state.saveConflict`(HTTP 409検知フラグ)から単一の状態(synced/local-only/syncing/offline/conflict)を算出。ヘッダーの`drawing-meta`(常時表示)へ`.save-status`バッジを追加し、色分け(緑=同期済み/黄=未同期・オフライン/青=同期中/赤=競合)とtitle属性・aria-labelで理由を説明。設定ダイアログの`system-summary`(旧「保存先: このブラウザ」固定文言+別枠の「API: 接続済み/未確認」)も同じ`saveStatusInfo()`を参照するよう統合し、矛盾する表示を解消。`apiRequest()`がHTTPステータスをError.statusとして保持するよう修正し、`commitCommands`が409(版競合)を検知して`saveConflict`をセット、`checkApiHealth`成功時にクリア。回帰テスト: E2E 2件(サーバー同期済み表示の確認、`page.route`によるネットワーク遮断でオフライン表示に切り替わることの確認) |
| P0-26 | 旧「Neon同期」UI文言の整理 | 全利用者/文書・UI正本の信頼性 | 80-90%方針§12直近アクション#12「現行評価書、README、UIに残る旧『Neon同期』表記の整理」への対応 | 低 0.1日 | P2 | なし | UI上の同期完了メッセージがNeon非言及になっている | 完了(2026-08-30): `src/app.js`の`persist("... / Neon同期")`(AI提案適用時・commitCommands成功時の2箇所)を`"... / サーバー同期"`へ修正。docs配下の「Neon」言及は全て2026-08-30以前の移行経緯の歴史的記録であり対象外(意図的に保持) |
| P0-27 | ヘッダーUI改善一式(ユーザー要望) | 全利用者/視認性・操作性向上 | ユーザー指摘: クイックアクセスアイコンが小さい、ファイル名がImportに反映されない、バージョン表示が分かりにくい、権限が視認しにくい、favicon/ブランドアイコンがない | 低 1日 | P1 | なし | アイコン拡大、Import時の図面名反映、バージョン表示改善、権限バッジ表示、favicon/ブランドアイコン追加 | 完了(2026-08-30): `.quick-btn`を30px→40px・アイコンSVGを14px→20pxへ拡大。`update_drawing_meta`opを新設しDXF/JSON Import時にファイル名(拡張子除く)を図面名へ反映。ヘッダーの`v${version}`を`Version ${version}`へ文言変更(図面バージョン番号自体は維持、ユーザー承認済み)。権限セレクトの左に`.role-badge`で現在ロール名を常時表示。SVGブランドアイコンをfaviconとヘッダー左上の両方に追加。モバイル幅でのレイアウト崩れとバッジのコントラスト比不足(WCAG AA未達)をE2E/A11yテストで検出し修正。回帰テスト追加 |
| P0-28 | ライトモード/ダークモード切替 | 全利用者/視認性・利用環境対応(ユーザー要望) | ユーザー指摘: 「全体のライトモード/ダークモードがその場で設定出来るようにしてください」 | 中 1日 | P1 | CSS変数体系の拡張 | 設定ダイアログでシステム設定/ライト/ダークを選択でき、選択が即時反映・再読込後も保持される。ダークモードでもA11y critical/serious違反0件 | 完了(2026-08-30): `src/styles.css`の`:root`変数を拡張し`@media (prefers-color-scheme: dark)`と`:root[data-theme="dark"]`の両方でダーク値を定義。`state.settings.theme`(system/light/dark)を新設しLocalStorageへ永続化。設定ダイアログの`#themeSelect`はchangeイベントで即時反映(適用ボタン不要)。レイアウト用紙(.layout-page等)は「紙」を表現するためテーマに関わらず固定のライト配色を維持(意図的)。回帰テスト3件追加(ダークモードA11y、切替の即時性・永続性) |
| P0-29 | 設定ダイアログにAIモデル接続設定(実験的)を追加 | 全利用者/将来のAI連携準備(ユーザー要望) | ユーザー要望「AIモデルAPIトークン設定も出来るようにしてください」。ユーザーが伏せ字入力・テスト/保存/クリアボタンを明示指定し、ブラウザ保存のみ・サーバー送信なしのセキュリティリスクを理解した上で実装を承認 | 中 1日 | P2 | 現状AI機能は外部LLM未使用(ルールベース) | APIキー欄がtype="password"、プロバイダ選択(OpenAI/Anthropic/カスタム)、接続テスト/保存/クリアがLocalStorageのみで完結し再読込後も保持される | **失効(2026-08-30)**: ユーザーが外部LLM対応を「サーバー側プロキシ方式」で明示的に依頼(AskUserQuestionで確認済み)したため、本機能は80-90%方針§5.6「ブラウザ及び配布ソースへAPIキーを置かない」と直接矛盾する設計となり不要化。CSP(`connect-src 'self'`)により本番では動作しないスタブでもあった。`state.aiSettings`/`AI_PROVIDERS`定数・関連4イベントリスナー・5関数(loadAiSettings等)を全削除し、起動時に旧LocalStorageキー(`mirai-web-cad-ai-settings`)を削除するコードへ置換。設定ダイアログは読み取り専用のサーバー状態表示(`GET /api/ai/status`、鍵は返さない)に変更。詳細はP0-34・P0-35を参照 |
| P0-30 | `/src/*`のエッジキャッシュがデプロイ後も古いJS/CSSを配信 | 全利用者/デプロイ即時性 | ユーザー報告「変わったように見えません」。UI改善(P0-27〜29)を本番反映したが、Cloudflareのエッジ(CDN)がオリジンのCache-Control欠如を拡張子ベースの既定TTL(4時間)で補完し、デプロイ前の`styles.css`(最大約2時間遅れ)を配信し続けていた | 低 0.3日 | P0 | Cloudflare Zone設定(Browser Cache TTL) | `/src/*`がCloudflareにキャッシュされず、デプロイ後は毎回オリジンへ再検証される | 完了(2026-08-30): 緊急対応としてCloudflare API(`purge_cache`)で該当URLを強制パージし即時解消。`_headers`に`/src/* → Cache-Control: no-cache, must-revalidate`を追加したが、Zone設定`browser_cache_ttl=14400`(固定、Respect Existing Headersではない)がオリジンの`no-cache`を`max-age=14400`へ上書きすることが再デプロイ後に判明(`origin_cache_control`によるオーバーライドはEnterprise限定のため使用不可)。最終対応として、`mirai-web-cad.mirai-dx-platform.com`ホスト名限定(他サブドメイン非対象)のCloudflare Cache Rule(`http_request_cache_settings`フェーズ、`cache: false`)を追加し`/src/*`をエッジキャッシュから完全バイパス。適用後`cf-cache-status: DYNAMIC`、オリジンの`Cache-Control`がそのまま返ることを確認。**このCache Ruleはリポジトリ外(Cloudflareダッシュボード/API側)の設定のため、`docs/deployment-local.md`に記録**。回帰テスト`tests/http-bridge.test.js`新規追加(3件、オリジン側ヘッダーのみ検証) |
| P0-31 | 権限プルダウンがdisabledの理由が不明瞭 | 全利用者/権限UXの明確化 | ユーザー質問「権限のところのプルダウン選択は出来ませんか？」。本番(`AUTH_MODE=access`)では`roleLocked`によりロール自己申告を禁止(P0-14のfail-closed設計)しているが、その理由がUIに表示されていなかった | 低 0.1日 | P2 | なし | `roleLocked`時、`#roleSelect`のtitle/aria-labelに固定理由(現在ロール名・変更にはログインが必要な旨)を表示 | 完了(2026-08-30): `#roleSelect`のdisabled時、`title`と`aria-label`に「認証済みの権限「〇〇」に固定されています。変更するにはログインが必要です」を追加。P0-22のribbonButtonHtml理由表示と同じパターン。権限自体の切替不可はP0-14の意図した保護であり変更なし |
| P0-32 | AI操作履歴の閲覧UI | 全利用者/AI変更の追跡可能性(ユーザー要望) | 80-90%方針チェックリスト監査で「AIが何を変更したかの追跡」が未実装(データはあるがUIパネルなし)と判明、ユーザーが対応を明示的に依頼 | 低 0.3日 | P1 | なし | 新規ドックタブ「AI履歴」で`commandEvents`のsource=agentのみを新しい順に表示、日時・ラベル・追加/更新/削除件数を確認できる | 完了(2026-08-30): 追加APIなしで実装(`GET /api/drawings/:id`が既にcommandEvents込みで図面全体を返すため)。`DOCK_TABS`に1タブ追加、`aiHistoryDockHtml`新設。5タブ化に伴い`.dock-tabs`へ`overflow-x: auto`を追加しモバイル幅でのレイアウト崩れを予防。回帰テスト2件追加(commandEventsのsource記録検証、E2Eでのタブ表示確認) |
| P0-33 | 図面コメント機能 | 全利用者/レビュー・承認フローでの合意形成(ユーザー要望) | 80-90%方針チェックリスト監査で「図面コメント」がスキーマ(`drawing.comments`)は存在するが完全未使用と判明、ユーザーが対応を明示的に依頼 | 中 0.6日 | P1 | なし | `ROLE_POLICIES.canComment`でreviewer等がcanEdit=falseでもコメント可能、新op`add_comment`、専用API`POST /drawings/:id/comments`、Propertiesドックにコメント一覧・投稿フォームを追加 | 完了(2026-08-30): DBマイグレーション不要(`comments`は`content` JSONBへ丸ごと保存、スキーマ検証は`comments`を見ない)。`applyTransaction`はコメントのみのトランザクションを`canEdit`ではなく`canComment`で判定する分岐を追加、`next.comments ??= []`で旧データを防御。承認済み図面は既存のfail-closedチェック(`state==="approved"`)により新規コメントも自動的に拒否される(MVP方針通り)。監査ログ(`comment.added`)にはcommentId/entityIdのみを記録しコメント本文は含めない。回帰テスト: cad-core.test.js 6件(reviewer投稿可・viewer拒否・旧データ防御・存在しない図形参照時の警告・承認済み図面拒否)、api-handler.test.js 2件(reviewer投稿+監査ログ非漏洩確認・viewer拒否)、E2E 1件(コメント投稿→一覧表示) |
| P0-34 | ブラウザ側APIキー設定を廃止しサーバー管理へ移行 | 全利用者/外部LLM統合の前提整備(ユーザー要望) | ユーザーが外部LLM(OpenAI/Anthropic)対応を「サーバー側プロキシ方式」で明示依頼。ブラウザに一切APIキーを置かない設計への転換に伴い、既存のブラウザ側APIキー設定(P0-29)が不要化・方針矛盾となったため削除が前提条件 | 低 0.4日 | P1 | なし(次PR4のAI_PROVIDER等サーバー環境変数実装の前提) | `state.aiSettings`等のブラウザ側キー管理コードを全削除、`GET /api/ai/status`(鍵の有無・プロバイダ名・モデル名のみ返却)を新設、設定ダイアログを読み取り専用のサーバー状態表示に変更 | 完了(2026-08-30): P0-29失効に伴う削除PR。`AI_SETTINGS_KEY`/`AI_PROVIDERS`定数、`state.aiSettings`/`aiTestStatus`、設定ダイアログのAPIキー入力fieldset、関連4イベントリスナー、`loadAiSettings`/`saveAiSettings`/`readAiSettingsFromForm`/`clearAiSettings`/`testAiConnection`関数一式を削除。起動時に旧LocalStorageキー(`mirai-web-cad-ai-settings`)を削除する後始末コードを追加(try/catchで防御)。新設`GET /api/ai/status`は`canRunAi`権限を要求し、`AI_PROVIDER`/`OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`AI_MODEL`環境変数(未設定時は`enabled:false`)から有効状態・プロバイダ名・モデル名のみ返却、APIキー自体は一切含めない。`checkApiHealth()`内で取得し`state.aiStatus`へ反映。CSP変更不要(全通信が`/api/...`の同一オリジンのため)。回帰テスト: api-handler.test.js 3件(未設定時disabled確認・設定時のキー非漏洩確認・canRunAi権限要求確認)、E2E 2件(読み取り専用UI確認・旧LocalStorageキーの自動削除確認)。未使用化した`.ai-settings-actions`/`.ai-test-status*`CSSも削除 |
| P0-35 | OpenAI/Anthropicサーバー側プロキシによるAI提案統合 | 全利用者/AI機能の実用性向上(ユーザー要望) | ユーザー指摘「外部LLMは一切使わない完全ルールベースで、対応する自然言語指示は3パターンのみ」への対応。外部LLM対応をサーバー側プロキシ方式・OpenAI/Anthropicのみで実装する方針をAskUserQuestionで確認済み | 高 1.5日 | P1 | AI_PROVIDER等の本番環境変数は未設定のため現時点では`enabled:false`のまま(ルールベースAIのみ動作を継続)。人間判断でAPIキー・現行モデルID・プロバイダ側の学習利用オフ設定が必要 | ルールベースAIが`needs_input`を返した場合のみLLMへフォールバック、LLM出力は許可opホワイトリスト+cad-core.js既存ファクトリでの再構築+検証を通過したもののみ`applyTransaction`へ到達、人間承認は既存フロー(agent-runs)のまま無変更 | 完了(2026-08-30): 新規`src/ai-provider.js`(OpenAI/Anthropicへのfetchアダプタ、`describeAiConfig`/`createAiCompletion`、`AiProviderError`、`AbortSignal.timeout(20000)`、レスポンスサイズ上限65KB、プロバイダの生レスポンス・エラーメッセージは一切外に出さない)、`src/ai-proposal.js`(`buildSystemPrompt`でdrawing_context/user_requestをデータとして明示しPrompt Injection対策、`normalizeLlmProposal`でLLM出力を直接`applyTransaction`に渡さずid/style/metaをサーバー側で再生成、レイヤー存在・ロック状態・用紙範囲(X:0-12000/Y:0-7000)・文字列長を検証、最大20コマンド)を新設。`src/api-handler.js`の`agent-runs`をルールベース優先・LLMフォールバックのハイブリッドに拡張、モジュールスコープの簡易レート制限(既定10回/分、LLM呼び出し時のみ、`env.AI_RATE_LIMIT_PER_MINUTE`で調整)を追加。監査ログ(`agent.planned`)には`{status,engine,provider,model,promptChars}`のみを記録しprompt本文・LLM生応答は含めない。テスト用注入点`env.AI_COMPLETION`(既存`ACCESS_JWT_VERIFIER`と同パターン)によりネットワーク不要でテスト可能。`scripts/serve-production.mjs`の`validateEnv()`と`scripts/serve-local.mjs`に`AI_PROVIDER`/`OPENAI_API_KEY`/`ANTHROPIC_API_KEY`/`AI_MODEL`/`AI_RATE_LIMIT_PER_MINUTE`を追加(値はログ非出力、AI_PROVIDER設定時は対応する鍵とAI_MODELを必須化)。`src/app.js`の`planAiProposal`に`state.aiBusy`/`aiError`/`aiEngine`を追加しローディング・エラー・使用エンジン表示、`aiDockHtml`にLLM未設定時の案内を追加、`proposalHtml`に多層防御のoptional chaining追加。CSP変更不要(全通信が`/api/...`の同一オリジン)。回帰テスト: `tests/ai-proposal.test.js`新規11件(add/update検証、id/style破棄、delete_layer等の禁止op除外、用紙範囲外除外、ロック中レイヤー除外、20件上限、Prompt Injection対策の文言確認等)、`tests/api-handler.test.js`5件追加(ルールベース優先でLLM未呼び出し・LLMフォールバック+監査ログ非漏洩・範囲外/存在しないレイヤー除外・LLM障害時のfail-soft+エラー非漏洩・レート制限)。本番`AI_PROVIDER`等は未設定のまま据え置き、実際の有効化(APIキー投入・モデルID確定・学習利用オフ設定)は人間判断で別途実施予定 |
| P0-36 | 実プロバイダ疎通で判明したAI連携の互換性問題を修正 | 全利用者/外部LLM連携の実効性(本番有効化検証) | ユーザーが本番へOPENAI_API_KEY/ANTHROPIC_API_KEYを投入、CTOがAI_PROVIDER=anthropic/AI_MODEL=claude-sonnet-5を設定・実疎通確認した結果2件の不具合を発見 | 低 0.3日 | P0 | なし | 実Anthropic APIで座標明示リクエストがstatus=plannedかつ正しい形状のコマンドを生成する | 完了(2026-08-30): (1)Adaptive Thinking採用モデルでは`temperature`パラメータが非推奨で400エラーになるためOpenAI/Anthropic双方のリクエストから削除。(2)`COMMAND_SCHEMA`が緩すぎ(commands要素が`type:"object"`のみ)、LLMが独自のネスト構造(entity.type等)で出力しコード側の期待形状(entityType/layerId等のフラット構造)と不一致、全add提案が「不正または未対応の操作」として除外され続けていた。各op種別ごとの厳密なJSON Schema(oneOf構成)へ差し替え。修正後、実プロバイダで座標明示の直線追加(planned到達)、Prompt Injection的指示(「前の指示を無視してレイヤー削除」)への耐性(モデル自身が拒否+多層防御)を確認。本番のAI_PROVIDER=anthropicは2026-08-30付けで実際に有効化・稼働開始 |
| P0-37 | 描画ループへ空間インデックス(uniform grid)を導入 | 全利用者/大規模図面での描画性能(Issue #7) | 80-90%方針Phase 2「R-tree／quadtree等の空間インデックス、描画・hit-test・OSnap・窓選択で共通indexを利用」への着手。viewportカリング(entityBounds/boundsIntersect)は実装済みだが、毎フレーム全entityへ線形走査していたため大規模図面でボトルネックになり得た | 低 0.3日 | P2 | hitTest・OSnapへの適用は未着手(次回以降) | drawCanvasのentity走査がO(n)からグリッドセルの近傍のみに縮小され、既存の可視性判定結果(未対応形状は常に描画に含める fail-open)と完全一致すること | 完了(2026-08-30): 新規`src/spatial-index.js`(`buildSpatialIndex`/`queryBounds`、uniform gridベース、既定セルサイズ1000mm)。境界不明な図形(block等でbounds算出不能なもの)は`unbounded`リストとして常に結果へ含め、従来の`boundsIntersect(null, viewport)===true`のfail-open挙動を完全維持。`src/app.js`の`drawCanvas`で`state.drawing.entities`の参照が変わらない限りインデックスを再構築しない参照ベースのメモ化(`visibleEntities`)を追加し、パン・ズーム中の再構築コストを回避。回帰テスト`tests/spatial-index.test.js`新規7件(brute-force一致検証、範囲外除外、viewport=null時の全件返却、セル境界をまたぐ図形の重複排除なし、境界不明図形の常時包含、2000要素规模でのbrute-force一致、閉じたポリラインの複数セルまたぎ)。hitTest・OSnap・窓選択への適用は未着手、次回以降の課題として改善台帳に残す |
| P0-38 | 代替率測定インフラ(比較器・採点表・100図面台帳・対象/対象外定義)の構築 | 全利用者/80-90%代替方針Phase 0(ユーザー要望) | 80-90%代替方針Phase 0の完了基準4点のうち①③④(①100図面の台帳と利用許諾が存在する③未対応entityが無通知で消失しない④実装済み/限定対応/計画の表示が最新状態と一致する。②比較器と採点表は本ID内で完了)への対応。「実案件100図面の収集」自体は人間の実業務データが必要なため対象外とし、100図面到着時に即座に使える基盤(器)の構築に限定 | 高 2日(3PR分割) | P0 | 実案件図面は未着手(人間側のタスク)。許容差・重みは初期提案値であり実データ到着後の再校正が前提。未対応entityのL2化(構造化要約)はP0-39として別途着手 | `compareDrawings`が9軸で採点し同一図面で満点/差分で軸別に減点する。空台帳が検証を通り20/80枠・許諾未取得除外が機械強制される。`docs/compat-scope-and-scoring.md`が対象・対象外・許容差v0・再校正手順を定義。`dxf-roundtrip`が合格スコアを返さない | 完了(2026-08-30、比較器・採点表・100図面台帳・対象/対象外定義文書の3PR): 新規`src/drawing-compare.js`(`compareDrawings`/`pairEntities`/`entitySignature`/`IGNORED_FIELDS`、cad-core.jsのvalidateDrawingは変更せず独立モジュールとして実装、entity/座標/layer/block/文字/寸法/layout/線種/印刷の9軸で採点)、`src/compat-score.js`(`TOLERANCE_V0`/`AXIS_WEIGHTS`/`GRADE_THRESHOLDS`/`scoreComparison`/`summarizeCorpus`/`describeRubric`、閾値を1ファイルに隔離し再校正を審査可能にする設計)、`scripts/compat-report.mjs`(CLI、`--mode=json-roundtrip\|dxf-import\|dxf-roundtrip\|calibration`)。ペアリングはID完全一致→型+レイヤー名+`spatial-index.js`(P0-37成果)再利用の近傍探索という2段構えで、import由来のID再採番後も幾何一致で対応付けできることを確認済み。一意に定まらない場合は`ambiguous-pairing`findingを必ず出力(黙って選ばない)。`dxf-roundtrip`モードはスタブ化せず`exit code 2`+未実装メッセージを返す設計とし、テストで固定。回帰テスト: `tests/drawing-compare.test.js`13件、`tests/compat-score.test.js`8件、`tests/compat-report.test.js`3件。**追記(2026-08-30、100図面台帳)**: 新規`scripts/lib/corpus-ledger.mjs`(`validateLedger`/`measurableEntries`/`computeStats`/`renderMarkdown`/`buildEntry`/`nextEntryId`)、`scripts/corpus-ledger.mjs`(CLI: `add`(既定dry-run、`--write`で追記)/`validate`/`stats`/`render`/`verify-files`)、`docs/compat-corpus/ledger.json`(`entries: []`で開始、現状0件でも`validateLedger`を通過する設計)、`docs/compat-corpus/README.md`(スキーマ・運用手順)。実図面はリポジトリにコミットせず`relativePath`+`sha256`+統計値のみ保持、実体は`MIRAI_CORPUS_DIR`環境変数が指すローカルディレクトリに置く(`.gitignore`に`corpus/`追加、著作権・機密保持のため)。`license.status`が`granted`/`internal`でない、または期限切れのentryは`measurableEntries()`が機械的に除外し、完了基準①「利用許諾が存在する」を検証可能にする核心設計。20/80枠(regression/uat)・id重複・パストラバーサル・sha256形式もバリデータが強制。回帰テスト`tests/corpus-ledger.test.js`13件(現行の空台帳がvalid・id重複拒否・20/80/100超過拒否・許諾未取得と期限切れの除外・パストラバーサル拒否・sha256形式検証・out-of-scope理由必須等)。**追記(2026-08-30、対象/対象外定義文書)**: 新規`docs/compat-scope-and-scoring.md`(80%必須範囲9項目・90%追加要件11項目・恒久的対象外・Phase 0測定対象外の線引き、採点9軸定義、許容差表v0、合否判定式、未対応entity非破棄ポリシーL0〜L3を集約。「対象外の追加はPR+改善台帳記録を必須とする」旨を明記し事後拡大を防止)。`README.md`の「DXF書出しは対象外」という表記を「実装予定(Phase 1、ADR-0001でライセンス取得を前提条件とする案を推奨、契約未着手)」へ是正(P0-40相当、既存の「未実装」表記(README Import行)とは矛盾していなかったため当該1箇所のみ修正)。`docs/operations.md`の「AIは外部LLMではなくルールベースのMVP提案」という記述をPR#47/#49で既に有効化された実際の挙動(ルールベース優先・LLMフォールバック)に同期。`docs/adr/ADR-0001-...md`へ補遺追記(ステータス「提案中」は変更せず、Phase 0はASCII DXFのみ受入としDWGバージョン確定はライセンス取得後ADR-0002へ持ち越す旨を明記)。**importers.jsのL2化(P0-39)は別ID、未着手** |

| P0-41 | DWG対応を方針から撤回しDXF単体運用へ確定(ADR-0002) | 全利用者/ODAライセンス依存の解消(ユーザー要望) | ユーザーからODAライセンス依存を回避する代替案2点(自作DXFパーサー/ジェネレーター、ezdxf等のOSS利用)の検討を依頼。調査の結果、両案ともDWGには対応せず(ezdxfのDWG対応はODA File Converter委譲のみ、自作DWGはLibreDWGの実例から非現実的)、DXF単体なら両案とも実現可能と判明。AskUserQuestionでユーザーが撤回を確定 | 低 0.5日 | P0 | なし | 80-90%代替方針・ADR-0001・compat-scope-and-scoring.md・README・operations.md・台帳スキーマ・Issue #6のDWG関連記述がDXF単体運用へ統一されている | 完了(2026-08-30): [ADR-0002](adr/ADR-0002-dwg-scope-drop-dxf-only.md)新規作成、ADR-0001ステータスを「破棄(Superseded)」へ変更。方針文書16箇所・compat-scope-and-scoring.md・README.md・operations.md・改善台帳(本ファイル)のDWG関連記述をDXFへ統一。`scripts/lib/corpus-ledger.mjs`/`scripts/corpus-ledger.mjs`/`tests/corpus-ledger.test.js`/`docs/compat-corpus/README.md`から`file.originalDwgVersion`フィールドを削除(台帳0件のためデータ移行不要)。DWGファイル拒否ロジック・テスト(`tests/importers.test.js`、`tests/corpus-ledger.test.js`の拡張子検証)は変更なし。GitHub Issue #6をリタイトルしDXF往復の実案件認定として継続(closeしない)。代替率数値の再計算は次回監査サイクルへ持ち越し。`docs/production-readiness-assessment.md`のDWG「重大なギャップ」評価は方針転換を踏まえ訂正(全体スコア再計算は別タスク) |
| P0-42 | Entra IDグループ所属によるRBAC動的マッピング(Issue #5) | 全利用者/案件単位RBAC(ユーザー要望) | ユーザーへ「Microsoft365非対話式認証でもよいか」の対象範囲を確認した結果、利用者ログイン自体はCloudflare Accessを維持しEntra IDへ差し替えない、案件単位RBACのためのグループ所属取得のみをMicrosoft Graph APIへ非対話式(client credentials flow)でアクセスする方針に確定。現行MVPのログイン方式はCloudflareアカウント認証。Entra ID管理者がApp Registrationを作成し`ENTRA_TENANT_ID`/`ENTRA_CLIENT_ID`/`ENTRA_CLIENT_SECRET`を本番へ投入済み(値は未確認、キー存在のみ確認) | 中 1日 | P1 | グループ⇔ロール対応表(`ENTRA_GROUP_ROLE_MAP`の実際のグループGUID)は実装と並行して決める方針のため未投入。Client Secretの有効期限(1年)のローテーション運用は当番体制側の残課題 | `resolveActor`がACCESS_ROLE_MAP不一致時にEntra IDグループ所属からロールを解決し、解決失敗時はACCESS_DEFAULT_ROLEへfail-closedに縮退する。複数グループ該当時は最高権限ロールを採用する | 完了(2026-08-30): 新規`src/entra-graph.js`(`src/ai-provider.js`と対になる構成、`describeEntraConfig`/`createEntraGroupResolver`、client credentials flowでのトークン取得+Microsoft Graph `memberOf`呼び出し、インメモリTTLキャッシュ2種(アプリトークン・ユーザー毎グループ、既定15分)、`AbortSignal.timeout`、応答サイズ上限、fail-soft)。`src/api-handler.js`の`resolveActor`へ`ACCESS_ROLE_MAP`(最優先、無変更)→Entra IDグループ(`ENTRA_GROUP_ROLE_MAP`、`ROLE_PRECEDENCE`で最高権限採用)→`ACCESS_DEFAULT_ROLE`(無変更)の3段階解決を追加。`scripts/serve-production.mjs`の`validateEnv()`へEntra 3変数の条件付き必須化・`ENTRA_GROUP_ROLE_MAP`のJSON/ロール名検証を追加(`ACCESS_ROLE_MAP`と同型)。`scripts/serve-local.mjs`にも同環境変数を追加。定期ジョブ(systemd timer)やDB新設テーブルは採用せず、既存の`env注入+fail-soft`パターン(`ACCESS_JWT_VERIFIER`/`AI_COMPLETION`)へ完全準拠させ運用変更を最小化。回帰テスト: `tests/entra-graph.test.js`新規12件(設定判定・キャッシュ・fail-soft全パターン)、`tests/api-handler.test.js`3件追加(複数グループ→最高権限選択・ACCESS_ROLE_MAP優先・解決失敗時のfail-closed)。`docs/deployment-local.md`/`docs/operations.md`へ設定手順・運用注記を追記。実際の`ENTRA_GROUP_ROLE_MAP`投入と本番疎通確認は人間側タスクとして残る |

| P0-44 | OSnap拡張(端点/中点/中心/四分点/交点+垂線/近接点のモード別設定) | 全利用者/正確な2D作図の補助 | カタログA-2「端点・中点・交点・垂線・接線・近接点・グリッド等へのオブジェクトスナップ」と方針文書Phase 1の「主要OSnap」への着手。従来は端点・頂点・中心・グリッドのみで、交点・中点・垂線・接線・近接点は未対応だった | 中 1日 | P1 | なし | 設定ダイアログ「OSnap対象」でモード別ON/OFFを永続化でき、スナップ動作が選択モードに従う | 完了(2026-09-04): `src/cad-draft-helpers.js`を拡張(`entitySegments`/`closestPointOnSegment`/`perpendicularFoot`/`segmentIntersection`/`OSNAP_MODES`/`DEFAULT_OSNAP_MODES`新設。`findOsnapPoint`をモード別候補選択へ改修し、既定=端点/中点/中心/四分点/交点ON・垂線/近接点OFF、戻り値は従来契約どおり座標のみ)。`src/app.js`: DEFAULT_USER_SETTINGSに`osnapModes`追加、設定ダイアログに「OSnap対象」チェックボックス群、`sanitizeOsnapModes`/`osnapModesFromForm`新設、snapPointから選択モードを渡す。回帰テスト: `tests/cad-draft-helpers.test.js`+10件(セグメント分解/垂線の足/交点の共有端点除外/中点・交点・垂線・近接点スナップ/モード無効化)、e2e +2件(OSnap対象の選択・保存・再読込)。残タスク: 接線(tangent)スナップ、極トラッキング、追跡線、OSnapの空間インデックス最適化(性能) |
| P0-45 | 精密編集コマンド追加(MIRROR/ARRAY/BREAK/JOIN) | CAD担当/施工図の精密編集 | カタログA-2「移動、複写、回転、鏡像、配列、ストレッチ、トリム、延長、分割、結合、オフセット」のうち、方針文書Phase 1で必須としつつ未実装だった鏡像・配列・分割・結合を追加 | 中 1.5日 | P1 | なし | MIRROR(軸2点)/ARRAY(矩形配列)/BREAK(line・開polyline分割)/JOIN(同一直線2線分結合)がコマンドラインとリボン「修正」から動作する | 完了(2026-09-04): `src/cad-advanced.js`へ`mirrorEntity`(線対称変換。rectは閉polylineへ型変換して置換)/`arrayEntity`(元位置を除くcols×rows-1複写、新ID採番)/`breakEntity`(分割点で2図形化。新ID採番により重複IDを防止)/`joinLines`(端点一致+同一直線のみ結合)を追加。`src/cad-command.js`: MIRROR/ARRAY/BREAK/JOINコマンドとHELP更新。`src/app.js`: OPERATION_LABELSとリボン「修正」に4ボタン追加(アイコン4種新設)、`parseTwoPointsValue`/`parseArrayValue`新設、applyOperationFormへ統合(型変換を伴う鏡像はdelete+add)。回帰テスト: `tests/cad-advanced.test.js`+12件、`tests/cad-command.test.js`+1件(統合フロー)、e2e +2件(コマンドライン駆動でMIRROR/ARRAY/BREAK/JOIN)。残タスク: STRETCH/EXPLODE/FILLET/CHAMFER/MATCHPROP、境界交点演算による正確なTRIM/EXTEND(方針文書Phase 1・P1-02)。**真の平行OFFSETはP0-46で対応済み** |
| P0-46 | ポリライン/ハッチの真の平行オフセット(miter join)化 | CAD担当/施工図の精密編集 | カタログA-2「オフセット」と方針文書Phase 1の精密編集。従来はpolyline/hatchを重心基準の放射移動で処理していたため、コーナーで真の平行曲線から形状が歪んでいた | 中 1日 | P1 | なし | polyline/hatchのOFFSETが真の平行オフセット(miter join)になり、直線形状の辺が全て元図形と平行を保つ | 完了(2026-09-04): `src/cad-advanced.js`の`offsetEntity`をpolyline/hatchについて書き換え、新規`parallelOffsetPoints`(各セグメントを法線方向へ平行移動し隣接オフセット直線の交点で頂点を再構成するmiter join)を実装。閉ポリラインは符号付き面積から内外を判定しdistance>0で外側・<0で内側へ。開ポリラインは進行方向の左側(正)へ(lineと同符号規則)。ハッチ境界は閉領域として扱う。`lineIntersection`ヘルパーを追加し不要になった`centroid`を削除。回帰テスト: `tests/cad-advanced.test.js`+7件(CCW矩形外側/CW矩形内側/閉polyline/開L字/hatch外側/斜め三角形の辺平行性/0長・縮退入力)。e2eは既存OFFSET(矩形対象)が影響なしを確認。残タスク: 円弧バルジ・自己交差形状のオフセット(P1-02)。**境界交点演算による正確なTRIM/EXTENDはP0-47で対応済み** |
| P0-47 | 境界交点演算による正確なTRIM/EXTEND | CAD担当/施工図の精密編集 | カタログA-2「トリム、延長」と方針文書Phase 1の精密編集。従来はTRIM/EXTENDが「選択線分の線端をクリック点へ座標移動」する簡易実装で、境界図形との交点で切る/延ばすというCAD本来の動作ではなかった | 中 1.5日 | P1 | なし | TRIMは境界(他図形)との交点で対象線分をクリップし、クリック点を含む側を残す。EXTENDはクリック点に近い端点を境界まで延長する | 完了(2026-09-04): `src/cad-advanced.js`へ`collectBoundarySegments`(line/rect/polyline/hatch境界のセグメント平坦化)/`parameterizeOnSegment`/`segmentIntersectionParam`(両セグメントのパラメータ付き交点、共有端点除外)/`trimEntityToBoundaries`(クリック点を含む区間を交点でクリップ、クリック点は線分上かつ垂直距離1%以内)/`extendEntityToBoundary`(クリック点に近い端点を延長レイ上の最初の境界交点まで移動)を追加。`src/cad-command.js`と`src/app.js`のTRIM/EXTENDを新実装へ置換(境界=選択図形以外の図面内エンティティ、操作値はクリック点x,y)。`editLineEndpoint`は残置(cad-advanced.test.jsが直接検証)。回帰テスト: `tests/cad-advanced.test.js`+8件(境界セグメント平坦化/右側・左側クリップ/複数交点/交差なし・線分外クリック拒否/近い端点への延長/延長先なし拒否)、e2e +2件(コマンドライン駆動でTRIM→EXTENDの境界交点編集)。残タスク: polylineのクリップ・延長、円・円弧境界、STRETCH/EXPLODE/FILLET/CHAMFER(P1-02) |
| P0-48 | CHAMFER/FILLET/BOUNDARY/PEDITの追加 | CAD担当/施工図の精密編集 | カタログA-2「面取り、フィレット、境界作成、ポリライン編集、頂点編集」とPhase 1精密編集の残ギャップを縮小 | 中 1.5日 | P1 | ネイティブ円弧entityは未実装 | 4コマンドがCLIとリボンから実操作でき、不正形状を明示エラーで拒否する | 完了(2026-09-05): `src/cad-advanced.js`へ`chamferLines`/`filletLines`/`createBoundaryEntity`/`editPolyline`を追加。面取りは直線2本、フィレットは接線円弧の16分割polyline、境界は接続線分3本以上、PEDITはMOVE/ADD/DELETE/CLOSE/OPENに限定。`src/cad-command.js`と`src/app.js`へCLI・リボン・操作フォームを接続しHELP更新。単体・CLI統合・E2Eを追加。残タスク: STRETCH/EXPLODE/MATCHPROP、ネイティブ円弧、Canvasグリップ、曲線PEDIT(P1-02) |
| P0-49 | 開発サーバーの自動割当LAN IP対応 | 開発者/同一LAN上の端末確認 | `0.0.0.0`待受済みでも起動表示が`127.0.0.1`固定で、DHCP割当IPの確認が別途必要だった | 低 0.3日 | P1 | OSファイアウォール・LAN分離設定 | 起動時に有効な物理インターフェースのIPv4を検出し、アクセスURLを表示する | 完了(2026-09-05): `scripts/lib/network-addresses.mjs`を追加し、内部・Docker bridge・veth・リンクローカルを除外してLAN IPv4を自動検出。`scripts/serve-local.mjs`は`Local`と`Network (interface)` URLを表示し、`HOST`で待受制限も可能。`eno1=192.168.0.185`とloopbackの両方でhealth応答を確認。単体テスト2件、`npm run verify`成功 |
| P0-50 | Cloudflare MVP専用URLとローカルDB分離 | CAD利用者/評価担当 | LAN URLでは社外から利用できず、既存本番と評価データが混在する | 中 1日 | P0 | Cloudflare Tunnel/Access、ローカルPostgreSQL | 指定URLがHTTPSで利用でき、許可メール1件だけがアクセス可能。MVP専用DBへ保存し、公開URL E2Eが成功する | 完了(2026-09-05): `mirai-web-cad-mvp.mirai-dx-platform.com`を既存Tunnelへ追加。Accessはホスト全体を`kensan1969@gmail.com`だけallow。`mirai-web-cad-mvp.service`(127.0.0.1:18813)とDB`mirai_web_cad_mvp`を新設。desktop/mobile公開URL E2E成功、テスト用Service Token/policyは削除済み |
| P0-51 | PostgreSQL JSONB二重保存の修正 | 全利用者/図面再読込み | postgres.jsへJSON.stringify済み文字列を渡し、図面・監査・AIデータがJSONB string scalarとして保存されていた | 高 0.5日 | P0 | 既存JSONB行の安全な正規化 | 新規データがobject/arrayで保存され、旧二重保存行も再読込み・migrationで正規化される | 完了(2026-09-05): `sql.json()`へ統一し、旧行を正規化する`0006_normalize_jsonb_columns.sql`と型検証を追加。MVP DB実測`content=object`/entities=1、PostgreSQL統合7/7、全verify成功 |
| P0-52 | 本番Migration 0006適用とMVPバックアップ・監視 | MVP/本番利用者、運用担当 | 本番DBの旧JSONB形式を解消し、MVP専用DBにも日次保全・復元確認・公開境界監視を設ける | 中 1日 | P0 | ローカルPostgreSQL/systemd/Cloudflare Tunnel | 適用前backup、全JSONB string 0件、本番health成功。MVP日次backup、鮮度検査、隔離DB復元、15分healthが実機成功 | **完了(2026-09-05)**: 本番を事前backup後にmigrationし、図面版10件を含む全対象JSONB列のstring scalar 0件とhealthを確認。MVPへ日次backup(14日保持)、日次鮮度検査、週次隔離DB復元、15分ごとのローカルAPI/DB名/Access 302監視を追加し、全サービスを実機で成功確認 |
| P0-53 | ネイティブARCとDXF円弧往復 | CAD担当/図形互換性 | DXF ARCをポリライン近似すると編集性と交換精度が失われ、FILLETも折線化されていた | 中 1日 | P1 | なし | ARCが内部モデル、作図UI/CLI、描画、選択、OSnap、変換、FILLET、JSON/DXF入出力、比較で同一型のまま動作する | **完了(2026-09-05)**: `arc` entity(中心/半径/開始角/終了角、度・反時計回り)を追加。象限をまたぐ境界/長さ/hit-test、端点/中点/中心/有効四分点OSnap、回転/尺度/OFFSET/鏡像、FILLETのネイティブARC生成、JSON/DXF ARC入出力、角度360度同値を考慮した比較を実装。Canvasは中心→始点→終点の3点作図、CLIは`ARC x,y radius startAngle endAngle`。MVPへ配備し、ローカル完全E2E 54/54、公開URLのAccess 302境界、MVP専用DB接続を確認。認証済み公開E2Eの再実行はAccess管理用API tokenの権限追加後に実施する。残タスクは円弧TRIM/EXTENDと曲線グリップ(P1-02) |
| P0-54 | Cloudflare Terraform化と障害Runbook | 運用担当/設定再現性と復旧時間 | Tunnel/DNS/Accessがダッシュボード操作とローカル設定に分散し、障害別の実行手順も単一章に混在していた | 中 1日 | P0 | Access Apps and Policies/DNSの管理権限は人間側のtoken発行待ち | Terraform fmt/validateがCI成功、既存資源をimport-firstで取り込める。MVPはメール1件のみ。サービス/Access/DB障害の完了条件をRunbook化 | **一部完了(2026-09-05)**: `infra/cloudflare/`へCloudflare provider v5.24のTunnel/DNS/MVP Access構成、importテンプレート、`prevent_destroy`、完全一致メール制約を追加。`infra:check`は全HCLを構文解析し、資源とimport単位で保護を検査。既存policy UUIDも棚卸し必須とした。CIでfmt/init/validate。サービス停止、Access変更、DB障害のRunbookを新設し、隔離DB復元でSHA-256・取得時刻・backup manifestとの4表件数/最新版署名・JSONB形状を実測照合。実資源import/plan/applyは権限付きtoken発行後に実施 |
| P0-55 | 外部入力の再棚卸しと再開条件固定 | 運用担当/推測設定と機密情報流出の防止 | 指定順序1〜3を実環境で再確認し、コードでは確定できない値を明示する | 低 0.5日 | P0 | Cloudflare管理token、Entra管理者、図面提供者、UAT責任者 | API応答・現件数・不足入力・再開条件を秘密値なしで記録 | **完了(2026-09-05)**: Cloudflare token verify=200、Access apps=200/0件、DNS=403(code 10000)を確認しimport/applyを停止。Entra OAuth token=200だが対象メールのGraph memberOf=404、`ENTRA_GROUP_ROLE_MAP`未設定。DXF台帳/実体/UAT参加者は0件/0名。詳細と安全な再開条件を`docs/external-input-status.md`へ記録 |
| P0-56 | ネイティブELLIPSE/SPLINEとDXF曲線往復 | CAD担当/曲線作図と交換精度 | 楕円・スプラインをポリライン近似せず、編集可能なネイティブ図形として保持する | 中 1.5日 | P1 | なし | UI/CLI、Canvas、選択、OSnap、変形、計測、JSON/DXF入出力、比較が同一型で動作 | **完了(2026-09-05)**: ELLIPSEは中心・長短半径・回転・開始/終了parameter、SPLINEは制御点・次数・clamped knotを保持。De Boor評価と曲線samplingを境界/長さ/hit-test/OSnapへ共通利用し、移動/回転/尺度/鏡像、JSON/DXFネイティブ往復、比較器へ接続。NURBS weight/fit point編集、曲線グリップ、曲線TRIM/EXTEND/OFFSETは後続 |
| P0-57 | CI依存関係監査ゲート追加 | 全利用者/供給網セキュリティ | 低 0.3日 | P0 | なし | `npm audit --omit=dev --audit-level=high`が必須チェックとしてbranch protectionに登録され、critical/high脆弱性混入時にmainマージをブロックする | **完了(2026-09-18、PR #96)**: 新規`dependency-audit`ジョブを追加し、`main`のbranch protection必須チェックへ登録(既存4件+本項目=5件)。開発依存の脆弱性はDependabot追跡のため非ブロッキング参考出力に留める。あわせてDependabot起票済みの`sharp`/`wrangler`既知high脆弱性2件をPR #91のマージで解消(`npm audit --audit-level=high`が0件になったことを確認) |
| P0-58 | PR #87(ネイティブDIMENSION/HATCH/VIEWPORT)のレビュー未完了・本番main分岐の解消 | CTO・レビュー担当/コード品質とガバナンス | 中 2-3日(CodeRabbit指摘14件の精査・修正・再検証) | P0 | 本番ホストのローカルmainが既にこの内容で稼働中のため、変更時は回帰確認必須 | CodeRabbit未解決14件(うち`src/cad-advanced.js`のsnapSpacing/gridSpacing座標変換誤り等、実バグと確認できたもの)を精査・修正し、全conversation threadをresolveした上でPR #87を正規のsquash mergeでmainへ反映する。本番ホストのローカルmain分岐(2026-09-18時点で暫定マージ済み)を解消し、以後の`scripts/deploy-local.sh`が`git merge --ff-only origin/main`で正常完了することを確認する | **完了(2026-09-18)**: ローカルbranchを`origin/main`へ追随させ、レビュー指摘の実バグ4件(HATCH elevationのgroup 10/20へのdx/dy書き戻し、原本パッチ経路でelevation.z(group 30)が書出されない、`encodeViewport`がsnapBase/snapSpacing/gridSpacing(13/14/15)を出力しない、angular寸法の13/14/15/16フォールバック縮退)を実行で再現して修正。さらに修正コミットへのCodeRabbit再レビューで新規未解決thread 8件が付き(`required_conversation_resolution`により正規手順でマージ不能)、8件すべてを実コードで再検証のうえ妥当と判断して修正(solidFill HATCHが面で塗られない、VIEWPORTのモデル空間座標を紙空間centerで回転・尺度変換、`entityLength(hatch)`が複数境界の周長の一部を無視、DXF type 5の3点角度寸法をtype 2として交差計算、角度寸法valueへのmeasurementScale未適用、ordinate寸法が`AcDbRotatedDimension`で出力、`encodeHatch`の境界未検証で"NaN"を出力、必須group codeを0で補い壊れたレコードを受入れ)。全threadへ修正内容を回答しresolve(未解決0件)。`npm run verify`全成功(ESLint 0 errors、unit 409件=408 pass/0 fail/1 skip、E2E 78/78)、新規回帰テスト9件は修正前に失敗し修正後に成功することを実測。**admin権限によるbranch protection迂回は一切行っていない。** 必須チェック5件+全ジョブ成功を`gh`で実測し、PR #87をsquash merge(`9849e6a`)。あわせて`npm run deploy:drift`が分岐状態でexit 1(fail-closed、`ok:false`)を返すことを実測した。**残作業**: 本番ホスト(`/home/kensan/Projects/Mirai-DX-Project/Mirai-Web-CAD`)のローカル`main`分岐の解消そのもの(別チェックアウトの操作。PR #87のマージにより`git merge --ff-only origin/main`が成立する状態になった) |
| P0-59 | 実質的な静的解析(ESLint等)の導入 | 全開発者/コード品質 | 中 2日(既存コードへのbreaking変更を避け段階導入) | P1 | 既存コードの警告量次第でルール調整が必要 | `npm run lint`が構文チェックに加え、未使用変数・複雑度等の静的解析をwarn中心で実行し、CIをブロックしない範囲で導入される | **完了(2026-09-18、PR #104)**。ESLint 10(flat config、`eslint.config.mjs`)を導入し、`npm run lint:static`として`verify:fast`(=CIのLintジョブ)へ組み込んだ。**実行時バグに直結する規則のみ**をerrorとし(未定義参照・重複キー/引数/クラスメンバ・到達不能コード・未使用変数・定数条件・`no-fallthrough`・`valid-typeof`・`use-isnan`・`no-self-assign`・`no-unsafe-negation`・`no-unsafe-optional-chaining`・`no-async-promise-executor`等)、整形・命名の規則は入れていない。`require-atomic-updates`はブラウザUIのイベントハンドラでは誤検知が多いため**warnのみ**(21件)としCIをブロックしない。導入時に検出した`FormData`未定義8件はグローバル定義の不足だった(実バグではない)。`scripts/check-cloudflare-iac.mjs`の`no-template-curly-in-string`は「HCL中の`${var.…}`という文字列そのもの」を探す検査のため誤検知として無効化。`npm run lint`にも`eslint.config.mjs`の存在検査を追加 |
| P0-60 | 認証モードのfail-closed化と公開オリジンのAPI遮断 | 全利用者/権限昇格の防止 | 低 0.5日 | P0 | なし(既存のaccessモード挙動は不変) | `AUTH_MODE`未設定・不正値が`access`として扱われ、`demo`は`APP_ENV=production`で拒否される。Pages Functionsは`access`以外を503で拒否する | **完了(2026-09-18、PR #99)**: 旧`authMode()`は未設定時に`APP_ENV!=="production"`なら`demo`を返し、`resolveActor`のdemo分岐が`x-demo-role`ヘッダーを無検証で採用していたため、**公開オリジン(Cloudflare Pages)で設定漏れがあると未認証の第三者が`cad_admin`相当へ到達し得た**。`access`/`demo`のみ受理するfail-closed実装へ変更し、`APP_ENV=production`でのdemo拒否、`functions/api/[[path]].js`の503ガード、`wrangler.toml`の`AUTH_MODE="access"`明示、回帰テスト10件(`tests/auth-hardening.test.js`)を追加 |
| P0-61 | 5xx内部エラー詳細の非公開化 | 全利用者/情報漏洩の防止 | 低 0.2日 | P0 | なし | 5xxの応答本文が`internal error`へ丸められ、DB接続エラー等の内部詳細が未認証クライアントへ返らない | **完了(2026-09-18、PR #99)**: 旧実装は`APP_ENV==="production"`のときだけ詳細を伏せていた。実測で`https://mirai-web-cad.pages.dev/api/health`が`{"ok":false,"error":"password authentication failed for user 'neondb_owner'"}`を**未認証で**返しており、内部の接続ユーザー名が露出していた。ローカル開発(demoかつ非production)以外では詳細を伏せ、原文はサーバーログにのみ残す |
| P0-62 | 本番デプロイ素性(稼働commit)の可視化と乖離検知 | CTO・運用担当/ガバナンスと事故検知 | 中 1日 | P0 | なし(ネットワーク非依存の判定を既定とする) | 稼働中のcommitが外部から確認でき、`origin/main`との乖離が機械的に検出される | **完了(2026-09-18、PR #99)**: 本番ホストのローカルmainが`origin/main`より4コミット先行し未レビューのPR #87コードが稼働していた事実(Issue #98)は、検知手段が無かったために人手の調査で初めて判明した。`GET /api/health`へ`deploy`(commit/branch/dirty)を追加、`scripts/lib/deploy-info.mjs`と`scripts/check-deploy-drift.mjs`(`npm run deploy:drift`)で`verified`/`behind`/`ahead`/`dirty`/`unknown`を判定、30分間隔のsystemd timerを追加、`serve-production.mjs`の起動時ガード(`DEPLOY_GUARD=strict`で起動拒否、既定は可用性優先で警告のみ)。回帰テスト17件(`tests/deploy-info.test.js`)。残: **稼働環境側の分岐解消は未実施**(P0-58が担当) |
| P0-63 | 監査probeの本番DB残留の解消 | 承認者・監査担当/監査証跡の完全性 | 低 0.3日(既存残留行の削除は別途承認要) | P0 | 既存行の削除には追記専用トリガーの一時解除が必要 | `db:verify`が検証対象DBへ恒久的な残留物を残さない。既存の残留行が削除され、監査証跡に合成行が存在しない | **一部完了(2026-09-18、PR #99)**: `scripts/verify-database.sh`が監査probe行をコミットしていたため、`scripts/deploy-local.sh`経由で本番`mirai_web_cad`へ「トリガーにより削除できない合成監査行」が混入していた(実測: `audit_trigger_verify_probe`、actor `verify`、2026-08-30)。probeを単一トランザクション+ROLLBACKへ変更し、残留ゼロでUPDATE/DELETE拒否を検証するようにした(`scripts/sql/verify-audit-append-only.sql`)。**残タスク**: 既存の残留1行の削除は追記専用トリガーの一時解除を伴う破壊的な本番DB操作のため、**承認を得てから**実施する(手順は`docs/operations.md`「監査ログの追記専用化」を参照) |
| P0-64 | API入力境界の検証強化(冪等キー順序・コマンド上限・expected-version・監査CSV) | 全利用者/操作不能の防止と入力起因の障害防止 | 低 0.5日 | P0 | なし(正常系の応答形状は不変) | 不正入力で誤った成功や恒久的な操作不能に陥らず、回帰テストで固定されている | **完了(2026-09-18、PR #100)**: ①案件系3経路が本文検証より先に冪等キーを予約していたため、本文不備の400がキーを焼き切り、同じキーでの正しい再送が恒久的に409となっていた(案件作成・メンバー追加が操作不能)。検証後に予約する順序へ変更。②`/drawings/:id/transactions`にコマンド上限が無く、`commands`非配列で`applyTransaction`の例外により500となっていた。上限500件・非配列400・超過413を追加。③`expected-version`を`Number()`で解釈し`1e0`/`0x1`/`1.0`が通っていたため`/^\d+$/`へ。④監査CSVの数式注入対策が先頭一致のみで先頭空白つき`" =cmd\|..."`が素通りしていたため最初の有意文字で判定。⑤未使用レガシー`saveDrawing`が`project_id`をデモ案件へ固定していた(RBAC破綻の潜在要因)ため呼び出し側の`projectId`を優先。`tests/api-hardening.test.js`10件を追加、`npm run verify`(unit 343件)・CI全ジョブ・Preview実測で確認 |
| P0-65 | API応答のセキュリティヘッダ付与と環境変数ファイル運用のfootgun解消 | 全利用者/多層防御と障害復旧の確実性 | 低 0.5日 | P0 | なし(アプリ設定ヘッダは上書きしない) | API応答とエラー応答にCSP/HSTS等が付与され、手動デプロイ手順が環境変数を壊さない | **完了(2026-09-18、PR #102)**: ①`_headers`が静的応答にしか適用されておらず`/api/*`と404/413/500にCSP/HSTSが無かった。`scripts/lib/http-bridge.mjs`の`applyEdgeHeaders`(不足分のみ補い、アプリ設定ヘッダは上書きしない)を新設し、本番・開発サーバー双方で適用。`_headers`にもHSTSを追加。②環境変数ファイルをシェルで`source`するとbashが代入値の引用符を除去し`ACCESS_ROLE_MAP`が不正JSONになる(実測36→32文字)ため手動起動が起動拒否で失敗していた。`docs/deployment-local.md`の手順を`sed`による1変数抽出へ変更し、起動拒否ログに原因の`hint`を追加(拒否自体はfail-closedとして維持)。実行時検証: `serve-production`を別ポートで起動し`/api/health`・401経路・SPAフォールバックのすべてでヘッダを確認。`tests/http-bridge.test.js`4件追加、unit 346件 |
| P0-66 | 濫用対策(更新系レート制限)と監査完全性の強化 | 全利用者/サービス継続と監査証跡の信頼性 | 低 0.5日 | P0 | 既定値は通常操作で引っかからない水準(240回/分)とし、環境変数で変更可能 | 更新系に上限があり、監査行の黙落ちが無く、`_headers`の書式違反がCIで検出される | **完了(2026-09-18、PR #103)**: ①レート制限がAI提案経路のみで図面更新・案件操作・監査出力は無制限だったため、`write`バケット(`POST`/`PATCH`/`PUT`/`DELETE`、既定240回/分、`WRITE_RATE_LIMIT_PER_MINUTE`)を追加。公開読み取りと`OPTIONS`は対象外。②状態のキー数が無制限に増え続けていたため上限`10_000`を設け期限切れ→最古順に破棄、`resetMemoryStore`でも消去。③`appendAudit`が`on conflict (id) do nothing`で衝突時に監査行を黙って落としていたため、挿入行数を確認し0行なら例外にして操作を失敗させる(メモリストアも重複IDを検出)。④`_headers`の書式違反行が黙って捨てられCSPが無言で欠落し得たため`findMalformedHeaderLines`を追加し`npm run lint`で検出。`tests/abuse-and-audit.test.js`8件追加、unit 357件 |
| P0-67 | 更新コマンドのop検証とSPAフォールバックの限定 | 全利用者/誤判定の防止と監視の成立 | 低 0.5日 | P1 | 既存の正常系(SPAが送る13 op)は不変 | 未知のopが400で拒否され、欠落アセットが404を返し、拡張子の無いパスは従来どおりindex.htmlを返す | **完了(2026-09-18、PR #105)**: ①`applyTransaction`は未知のopを黙って無視するため、綴り間違いでも200(成功)が返り「何も起きないのに成功」となっていた。解釈する13種(`add`/`add_comment`/`add_layer`/`delete`/`delete_layer`/`delete_selection`/`save_selection`/`set_block_resources`/`set_empty_drawing_unit`/`update`/`update_drawing_meta`/`update_layer`/`update_layout`)を許可リスト化し、未知のopは400で拒否。非オブジェクトのコマンドも400。②1コマンドあたりの`points`長上限`10_000`を追加(超過413)。③SPAフォールバックが拡張子を問わずindex.htmlを返していたため、`/missing.js`等の欠落アセットが200(text/html)になり読み込み失敗の検知も外形監視も成立しなかった。フォールバックを拡張子の無いパスに限定し、ファイル要求は404にした(実行時検証: `/missing.js`=404、`/deep/client/route`=200 html)。unit 366件 |
| P0-68 | 独立レビュー指摘の反映(図形検証・案件ACL fail-closed・冪等キー解放・保存失敗の可視化) | 全利用者/誤判定・権限・データ損失の防止 | 中 1.5日 | P0 | 既存の正常系は不変(update前後で新たに生じた構造不正のみ拒否) | 不正な図形を保存できず承認が500で固まらず、案件ACLがfail-closedで判定され、保存失敗が画面に出る | **完了(2026-09-18、PR #106)**: 実装者と別コンテキストの専門SubAgent2件による独立レビューを実施し、指摘を実コードで再検証した(3件は反証)。①`applyTransaction`が`command.entity`を無検証でpushするため`points`無しlineを保存でき、承認時の`validateDrawing`がTypeError→500となり削除以外で復旧不能だった。`boundsFromPoints`/`entityBounds`を防御的にし、更新で新たに生じた構造不正を保存前に400で拒否、`approveDrawing`の阻止条件に`invalid-geometry`を追加。②`probe()`がmigration 0007を検証せず、`requireProjectAccess`が`accessScope!=="restricted"`でfail-openだったため、0007未適用時に案件ACLが無効なまま健全と報告され得た。probeへ0007検証を追加し、ACL判定をfail-closed化。③業務処理失敗時に冪等キーの予約が残り正しい再送が恒久的に409となっていた。`releaseIdempotency`を追加し失敗時に解放。④`saveDrawing`の例外が伝播し「画面には反映されたが保存されていない」無言のデータ喪失になっていた。`{ok,reason}`契約へ変更し失敗を画面表示、`drawCanvas`を例外境界で隔離、ダウンロードの同期revokeを修正、`Permissions-Policy`のドリフトを全項目一致テストで検出。`tests/geometry-integrity.test.js`他 計17件追加、unit 379件 |
| P0-69 | アクセシビリティ通知の回復とレイヤ保護 | 全利用者(とくに支援技術利用者)/公共調達の適合 | 低 0.5日 | P1 | 既存のDOM構造と操作フローは不変 | エラーが支援技術へ伝わり、generic要素のaria-label違反が無く、ロック中レイヤを削除できない | **完了(2026-09-18、PR #107)**: ①`render()`が`#app`を`innerHTML`で作り直すためコマンドログの`aria-live`が描画ごとに破棄され、権限拒否・Import失敗・保存失敗が読み上げられなかった。`#app`の外に`#sr-announcer`(`role="status" aria-live="polite"`、`.sr-only`)を置き、render後に最新ログを流すようにした(E2Eで実ブラウザ検証)。②`#app`/`.drawing-meta`/`.ribbon`/`.command-history`/`.status-bar`はroleがgenericなのに`aria-label`を付けており`aria-prohibited-attr`(serious)に該当。`#app`は削除、他は`group`/`toolbar`/`log`を明示。③ロック中のレイヤーを削除できたため`delete_layer`で拒否。④用紙範囲(12000×7000mm)が`cad-core.js`と`ai-proposal.js`に重複定義されていたため`MODEL_EXTENT`へ統合。**なお「用紙境界がA3横mm固定」という指摘は反証**(12000×7000はアプリ全体の仮想シートで、物理用紙寸法に置換すると正常な図面が全て用紙外と誤判定される)。unit 383件・E2E 76件 |

| P0-70 | オフサイトバックアップの不在 | 全利用者/災害対策(DR) | 高 3日 | P0 | 転送先(Cloudflare R2等)の契約、暗号化・保持期間の方針合意 | 日次バックアップがホスト外へ暗号化転送され、ホスト全損時にも復旧できる | **未着手(2026-09-18 独立監査でCritical確定)**: バックアップは`/var/backups/mirai-web-cad/postgres/`(DBと同一ホスト・同一ディスク、14日保持)のみで、`rclone`/`rsync`/`s3`/`gpg`/`age`の類が0件。ホスト全損で本番とバックアップが同時に失われる。**要外部契約** |
| P0-71 | 本番DB復旧ドリルの初回準備(隔離DB作成) | 全利用者/復旧可能性 | 低 0.5日 | P0 | DB管理者による`mirai_web_cad_recovery`作成(本番接続ロールにCREATEDB権限が無い) | 週次timerが実際に成功し、RTOを実測できる | **一部完了(2026-09-18 第8ラウンド、CTO承認)**: 手動実行による復元は成功したが、週次timerのインストールとRTOの実測は未実施(systemd unitの作成はポリシーゲートで拒否されたため人間作業)。 隔離DB`mirai_web_cad_recovery`を`mirai_web_cad_backup`ロール所有で作成。`backup.env`へ`RESTORE_DATABASE_URL`を追加。**新規検出**: migration 0007の`project_members`にバックアップロールのSELECT権限が無く、**次回定時バックアップが失敗する**状態だった(P0-91として分離)。`GRANT SELECT`で修正後、**本番復旧ドリル実行成功**: `projects=1 drawings=10 versions=10 audits=16 invalid_json=0 latest_version_mismatches=0 manifest_match=yes backup_age_seconds=17`、exit 0。**バックアップが実際に復元可能であることを実証。オフサイト転送は未着手(要契約)** |
| P0-72 | リリース承認ゲートの不在 | CTO/変更統制 | 中(要判断) | P0 | GitHubプラン見直しまたはリリース承認手順の運用担保 | レビュー承認なしに本番投入できない | **未着手(2026-09-18 独立監査でCritical確定)**: `.github/workflows/production.yml`にデプロイジョブも`environment:`も無く、required reviewerはプラン制約で設定不能(P0-08の残)。CODEOWNERSは`@Kensan196948G`一人で実効化されない。唯一の抑止は`required_conversation_resolution`(AIレビュー)であり人間承認ではない。**要経営判断** |
| P0-73 | バックアップ/鮮度検査/復旧ドリル失敗の通知不在 | IT/DX 7名/検知 | 低 0.5日 | P0 | 通知先の確定(Issue #8) | 失敗が人手を介さず検知される | **未着手(2026-09-18 独立監査でHigh確定)**: `deploy/systemd/*.service`に`OnFailure=`が0件。合成監視(`synthetic-monitor.yml`)は公開HTTP境界のみで、バックアップ経路を監視しない。`docs/operations.md`は「バックアップ失敗=Sev2、鮮度検査が検知経路」と定義しているが通知実装が無い |
| P0-74 | `db:verify`が毎デプロイで本番DBへmigration+seedを適用 | 全利用者/本番データ保全 | 中 1日 | P0 | デプロイ手順(`scripts/deploy-local.sh`)の変更には手順書(`docs/deployment-local.md`)の同時更新が必要 | デプロイ時の検証が本番DBへ書き込まない | **完了(2026-09-25)**: `scripts/deploy-local.sh`を`db:verify`→`db:check`へ切り替え、手順書`docs/deployment-local.md`へ「Migrationを含むリリース」の事前適用手順を追記(文書と実装を同時更新)。切替前に本番DBで`db:check`が成功することを実測(tables=9 audit_triggers=3 jsonb_strings=0)。回帰テスト`tests/deploy-script.test.js`でデプロイ経路が`db:verify`を実行しないことを固定。以下は第1段階の記録。**一部完了(2026-09-18 第2ラウンド)**: 読み取り専用の`scripts/check-database-state.sh`(`npm run db:check`)を新設。migration作成物(9テーブル・`drawings.revision`/`visibility`・`projects.access_scope`)、監査追記専用トリガ2件とUPDATE/DELETE拒否(検査はROLLBACK)、JSONB string scalar 0件を検証し、未適用なら欠落を列挙して**exit 1**で失敗する(デプロイを止める)。**実測**: migration適用済DBで実行前後の行数と`content_hash` md5が完全一致(書き込みゼロ)、空DBではexit 1。**残作業**: `scripts/deploy-local.sh`の`db:verify`→`db:check`切替。手順書`docs/deployment-local.md`の同時更新が必要だが、本セッションのポリシーゲートがINFRA_CHANGEとして当該ファイルの編集を拒否したため**未実施**(文書と実装の矛盾を避けるため`deploy-local.sh`の変更も取り消した)。人間側で手順書を更新のうえ切替えること |
| P0-75 | 案件分離が実運用で機能していない | 全利用者/情報境界 | 中 3日 | P0 | 既定`restricted`化・案件選択UI・付け替えAPIのいずれを採るかの判断 | 案件ごとに独立したアクセス境界がある | **未着手(2026-09-18 独立監査でHigh確定)**: `projects.access_scope`の既定が`open`で、SPAの`POST /api/drawings`は`projectId`を送らないため全図面が単一のopen案件(`prj_demo_road_001`)へ集約される。結果、Cloudflare Accessを通った全ロール(viewer含む)が全図面を閲覧でき、drafter以上が全図面を変更できる。`requireDrawingAccess`自体は全経路でfail-closedだが、分離すべき案件が実在しない構成。**当面は受容リスクとして経営層承認が必要** |
| P0-76 | `isCadDrawing`失敗時にデモ図面を黙って返す | 全利用者/Data Loss | 低 0.3日 | P0 | なし | 破損図面は明示エラーになり、実データを上書きしない | **完了(2026-09-18 第2ラウンド)**: `src/data-store.js`の`getDrawing`が`isCadDrawing`失敗時に`seedDrawing()`へ差し替える挙動を削除し、明示的に例外を投げる(fail-closed)ようにした。APIは5xxとして扱い内部詳細を返さない。実PostgreSQL統合テストを追加(contentを`{broken:true}`へ差替え→`getDrawing`が`/解釈できません/`でreject)し、CI相当の専用ロール/DBで**9/9 pass**を実測 |
| P0-77 | `MAX_COMMAND_POINTS`が実ペイロード形状に効かない | 全利用者/資源保護 | 低 0.3日 | P0 | なし | 点列長上限が実クライアント経路で発動する | **完了(2026-09-18 第2ラウンド)**: `command.points`に加え`command.entity.points`(`op:"add"`)と`command.patch.points`(`op:"update"`)も検査対象にし、違反は413で拒否する。回帰テストは実形状(entity/patch)で上限超過を検証し、修正前は上限が発動しないことを確認済み |
| P0-78 | 監査の追記専用保護の穴(TRUNCATE・トリガ一時解除) | 監査者/改ざん耐性 | 中 1日 | P1 | 既存migrationの扱いとロール構成の変更 | TRUNCATEとトリガ無効化でも監査を改変できない | **完了(2026-09-18 第3ラウンド)**: (1) `migrations/0008_audit_truncate_guard.sql`で`before truncate ... for each statement`トリガを追加し、`reject_audit_log_mutation()`を再利用してerrcode 42501で拒否。あわせてUPDATE/DELETEトリガも冪等に再作成し、`0006`が中断した場合の回復経路とした(0006自体は履歴migrationのため変更せず)。(2) `scripts/sql/verify-audit-append-only.sql`と`db:verify`/`db:check`の期待値を3トリガへ更新し、TRUNCATE拒否も機械検証する。(3) 拒否理由の判定を「トリガ」または「権限不足」のいずれでも成立するよう修正(所有権分離後は権限が先に拒否するため、トリガのみを要求すると正しい状態を『保護が無効』と誤判定する)。(4) `scripts/sql/harden-audit-role.sql`(DB管理者が一度だけ実行)で`audit_logs`の所有権を専用ロールへ分離し、アプリ用ロールへSELECT/INSERTのみを付与する手順を用意。`db:check`は所有者が接続ロールと同一の場合に警告する。**実測**: トリガ3件が有効。所有者ロールでもスーパーユーザでもTRUNCATE/UPDATE/DELETEが拒否される。所有権分離後はアプリ用ロールでTRUNCATE/UPDATE/DELETEが権限拒否、SELECT/INSERTは成功。分離スクリプトは冪等(2回適用でexit 0)。`db:check`は両構成で成功。**運用帰結(実測)**: 所有権分離後は`db:verify`をアプリ用ロールで実行できない(`must be owner of table audit_logs`)。migration適用は所有者ロールまたは管理者で行う。**残作業**: 本番DBへの所有権分離の適用はDB管理者の承認が必要 |
| P0-79 | 設定の起動時検証と転送の不足 | 運用担当/設定ミス防止 | 低 0.3日 | P1 | なし | 誤設定が起動時に拒否される | **完了(2026-09-18 第2ラウンド)**: `scripts/serve-production.mjs`で`ACCESS_DEFAULT_ROLE`を`ROLE_POLICIES`と突合し、未知なら`exit 78`(他2つのロールマップと同じ扱い)。`viewer`以外なら「未マップの全利用者へ付与される」旨をwarnで記録する。あわせて`WRITE_RATE_LIMIT_PER_MINUTE`を起動時envへ転送し、`production.env`の設定が実際に反映されるようにした(handlerは元々envから読む実装だった)。回帰テストで未知ロール時のexit 78を実測 |
| P0-80 | 404本文差による存在オラクルとGETでの監査追記 | 全利用者/証跡の完全性 | 低 0.5日 | P2 | なし | 存在秘匿が本文まで一貫し、GETが状態を変えない | **完了(2026-09-18 第2ラウンド)**: (1) `requireDrawingAccess`の404本文を定数`図面が見つかりません。`に統一し、要求IDを本文へ含めないことで「存在しない図面」と「権限のない案件の図面」を**完全に同一の応答**にした(実測で本文一致)。(2) `GET /api/audit-logs?format=csv`を廃止して一覧は読取り専用とし、CSV出力を`POST /api/audit-logs/export`(`content-type: application/json`必須、415で拒否)へ移した。GETはCORSのsimple requestとして扱われるためクロスサイトの`<img>`/`<link>`から被害者名義で`audit.exported`を追記できた。回帰テストでGETが状態を変更しないこと、POSTは監査に記録されることを検証 |

| P0-81 | CIの`Deploy Preview`がDependabot起点のPRで常に失敗する | 運用担当/真の異常の判別 | 低 0.2日 | P1 | なし | Dependabot起点のPRで`Deploy Preview`が赤くならない | **完了(2026-09-18 第5ラウンド)**: Dependabot起点のPRにはリポジトリのシークレットが渡らない(GitHubの仕様)ため、`CLOUDFLARE_API_TOKEN`が空でwranglerが失敗し、**必須ではないジョブが常に赤**になっていた(P0-21と同種の「恒常的な赤が真の異常を覆い隠す」問題)。`Check Cloudflare credentials`ステップで資格情報の有無を判定し、無い実行では配信と検証をスキップしてnoticeで理由を残すようにした。実測: PR #89/#90 の`Deploy Preview`失敗ログが`CLOUDFLARE_API_TOKEN`空による`wrangler`エラーであることを確認 |
| P0-82 | `content_hash`が内容由来でなく監査・復元の署名が論理破損を検出できない | 監査者・承認者/改ざん・破損検知 | 中 2日 | P1 | 既存保存値との互換(段階移行が必要) | 復元時の署名照合が内容の論理破損を検出できる | **未着手(2026-09-18 独立監査でHigh確定)**: `data-store.js`の`content_hash`は`commandEvent.afterHash`(=entitiesのみを対象とする32bitハッシュ)または`version-N`であり、内容そのもののダイジェストではない。`scripts/database-signature.sh`はこの値をmd5するだけのため、**layers/用紙/尺度/単位の変更や論理破損を検出できない**。強ハッシュ化と対象拡張は保存済みハッシュとの互換に関わるため段階移行が必要 |
| P0-83 | migration版管理が無く`table_count == 9`が固定である | 運用担当/デプロイ継続性 | 中 1日 | P1 | `schema_migrations`導入と既存migrationのbaseline化 | テーブルを追加するmigrationを適用してもデプロイが失敗しない | **一部完了(2026-09-18 第6ラウンド)**: `scripts/verify-database.sh`の`table_count != 9`(件数の完全一致)をやめ、**期待テーブルを名前で検証**する方式へ変更した。実測: 10テーブルでも`db:verify`が成功し、期待テーブルを落とすと`db:check`が欠落名を列挙してexit 1で失敗する(検知力は維持)。**残作業**: `schema_migrations`による版管理と、毎回全migrationを再適用する運用の見直し(本番DBの状態を変えるため要判断) |
| P0-84 | backup/restoreに「対象DB名」のアサーションが無い | 運用担当/環境取り違え防止 | 低 0.5日 | P1 | なし | 本番用とMVP用のバックアップ/復元がDB名で検証される | **完了(2026-09-18 第6ラウンド)**: `backup-database.sh`が`EXPECTED_DATABASE`と接続先DB名の一致を必須にし(不一致はexit 4)、manifestへ`database=`と`pg_client=`を記録する。`restore-database.sh`は`EXPECTED_DATABASE`設定時にmanifestの`database=`一致を必須とする(過去のmanifestには無いため未設定なら警告)。本番/MVPのbackupとrestore-drillの4 unitへ`EXPECTED_DATABASE`を設定。**実測**: 不一致でexit 4、一致で成功しmanifestに`database=`が入る、restoreは不一致のmanifestをexit 4で拒否 |
| P0-85 | 監視がGitHub Actionsの分数予算に100%依存している | IT/DX 7名/可用性監視 | 中 0.5日 | P0 | 予算確認または本番ホスト側の監視unit追加 | 監視がGitHub Actionsの分数超過で同時に沈黙しない | **未着手(2026-09-18 独立監査でHigh確定)**: `synthetic-monitor.yml`は15分間隔(約2,880分/月)。契約プランによっては無料枠を超え、超過時にActionsが停止して**監視・CI・Dependabotが同時に沈黙**する。本番ホスト側の可用性監視unitはMVP専用(`18813`)のみで、本番`18812`を常時監視するunitが無い。全unitに`OnFailure=`も無い。**契約プランの実測が未確認** |
| P0-86 | ロールバックがDBを戻さず、復旧自体も壊れ得る | 全利用者/復旧可能性 | 中 1日 | P0 | 手順書の同時更新(デプロイ手順) | デプロイ失敗時にコードとDBが整合した状態へ戻る | **未着手(2026-09-18 独立監査でHigh確定)**: `deploy-local.sh`の`rollback()`は`git checkout`+`npm ci`+`build`+`restart`のみで、**DBスナップショットを取らずDBを戻さない**(migrationは前進したまま)。また`npm ci`は`node_modules`を先に削除するため、NW断時は旧コードも起動不能になり得る。`curl`に`--max-time`が無くhealthループがハングし得る。バックアップは日次1回のため日中失敗時は復元点が最大24時間前 |
| P0-87 | 本番ホスト構成がコードで再現できない | 運用担当/復旧時間 | 高 3日 | P1 | 手順書・IaCの整備 | 新規ホストを手順だけで復旧できる | **未着手(2026-09-18 独立監査でHigh確定)**: PostgreSQLロール/権限、`~/.cloudflared`実設定(`deploy/cloudflared`は`.example.yml`のみ)、`~/.config/*.env`、`/var/backups`作成がすべて手作業。ホスト再構築用Runbookが無い。さらに`deploy/systemd/*.service`の`WorkingDirectory`は`/home/kensan/Projects/Mirai-DX-Project/Mirai-Web-CAD`を指しており、本リポジトリのチェックアウトとは別パス(要ホスト確認) |
| P0-88 | 供給網統制の欠落(SBOM・ライセンス検査・CODEOWNERS・actionsのSHA固定) | CTO/供給網 | 低 2日 | P2 | 法務方針 | SBOMとライセンス一覧がCIで生成される | **一部完了(2026-09-18 第6ラウンド)**: CI(`.github/workflows/ci.yml`)へ`sbom`ジョブを追加し、`npm sbom --sbom-format cyclonedx`でSBOMを生成・検証して`actions/upload-artifact`で保存する。**実測**: ローカルで131コンポーネント(production-scope 4)のCycloneDXを生成し、検証コマンドが成功。**残作業**: ライセンス方針の決定(要法務)、CODEOWNERSの実効化(プラン制約)、ActionsのSHA固定(要更新運用) |
| P0-89 | バックアップは成功するが復元できない(PGクライアント版の不一致) | 全利用者/災害対策(DR) | 低 0.5日 | P0 | なし | バックアップと復元が同じメジャー版のクライアントで行われ、復元が成功する | **完了(2026-09-18 第6ラウンド、実測で検出)**: サーバはPostgreSQL **16.14**だが`pg_config --bindir`は**18**。`pg_dump`(18)は`SET transaction_timeout = 0`(PG17以降GUC)を出力し、16のサーバへpg_restoreすると`unrecognized configuration parameter "transaction_timeout"`で失敗し復元先DBが空のまま残った。`scripts/lib/pg-bin.sh`を新設し**サーバのメジャー版からクライアントを解決**。manifestへ`pg_client=`を記録。**実測**: 修正前復元exit 1・復元先0テーブル → 修正後`pg_client=16.14`で復元exit 0・復元先9テーブル・`manifest_match=yes`。解決ロジック4分岐を個別検証 |
| P0-90 | 本番が検証不能なバージョンで稼働している(P0-58の残作業の精確化) | 全利用者/統制とセキュリティ修正の未反映 | 低 0.5日(作業自体) | P0 | 本番チェックアウトの分岐解消 | 本番が`origin/main`のレビュー済みcommitで稼働し、`deploy.commit`で素性を報告する | **完了(2026-09-18 第8ラウンド、CTO承認)**: 本番チェックアウトで`backup/pre-p090-reconcile-20260918`に現状保存後、`main`へ切り替え`git reset --hard origin/main`で分岐解消(分岐4コミットの内容はPR #87としてsquash merge済みのため損失なし)。`db:verify`でmigration 0008を本番DBへ適用(監査トリガー3件化)。`npm run build`後、本番プロセスをSIGKILLしsystemdの`Restart=on-failure`で反映。**実測**: `deploy.commit=91dea09`(=origin/main)、`deploy.branch=main`、`deploy.dirty=false`、`deploy:drift:live`が**一致**を報告、本番監視スクリプトexit=0 |
| P0-91 | migration追加時にバックアップロールへのSELECT付与が漏れる | 全利用者/バックアップ継続性 | 低 0.3日 | P0 | migrationでテーブル追加時にGRANTを含めるか`ALTER DEFAULT PRIVILEGES`を設定 | テーブル追加後もバックアップが失敗しない | **完了(2026-09-18 第8ラウンド、実測で検出)**: migration 0007で`project_members`追加時にバックアップロール(`mirai_web_cad_backup`)への`GRANT SELECT`が漏れていた。前回バックアップは0007適用前だったため気づかなかったが、**次回定時バックアップが`permission denied for table project_members`で失敗する**状態だった。`GRANT SELECT ON public.project_members TO mirai_web_cad_backup`(superuserで実行)で修正。**恒久対策**: migrationでテーブル追加時にバックアップロールへの権限付与を必須とするか、`ALTER DEFAULT PRIVILEGES`を設定する必要がある(要判断) |

| P0-89 | バックアップは成功するが復元できない(PGクライアント版の不一致) | 全利用者/災害対策(DR) | 低 0.5日 | P0 | なし | バックアップと復元が同じメジャー版のクライアントで行われ、復元が成功する | **完了(2026-09-18 第6ラウンド、実測で検出)**: このホストのサーバはPostgreSQL **16.14** だが、PATH上の`pg_config --bindir`は**18**を指していた。`backup-database.sh`/`restore-database.sh`は`pg_bin="${PG_BIN:-$(pg_config --bindir)}"`で既定を決めていたため、**PG_BIN未設定のドリル手順(README記載の`npm run db:backup`/`db:restore`)では、pg_dump(18)が`SET transaction_timeout = 0`(PG17以降のGUC)を出力し、PostgreSQL 16のサーバへpg_restoreすると `unrecognized configuration parameter "transaction_timeout"` で失敗して復元先DBが空のまま残った**。定時バックアップ(systemd unitが`PG_BIN=/usr/lib/postgresql/16/bin`を明示)は影響を受けないが、手順どおりの復元訓練が成立しない状態だった。`scripts/lib/pg-bin.sh`を新設し、**サーバのメジャー版からクライアントを解決**する(aptレイアウトを優先し、無ければ`pg_config`の版がサーバと一致する場合のみ採用。一致しなければexit 5で中止し、新しい版でのdump作成を防ぐ)。manifestへ使用クライアント版(`pg_client=`)を記録。**実測**: 修正前は復元exit 1・復元先0テーブル、修正後はPG_BIN未設定でも`pg_client=16.14`でバックアップされ復元exit 0・復元先9テーブル・`manifest_match=yes`。解決ロジックの4分岐(サーバ版一致/不一致/フォールバック一致/版取得不能)を個別に検証 |
| P0-90 | 本番が検証不能なバージョンで稼働している(P0-58の残作業の精確化) | 全利用者/統制とセキュリティ修正の未反映 | 低 0.5日(作業自体) | P0 | 本番チェックアウトの分岐解消(人間操作)。`db:verify`の本番適用を伴うため承認推奨 | 本番が`origin/main`のレビュー済みcommitで稼働し、`deploy.commit`で素性を報告する | **未着手(2026-09-18 第7ラウンドに実測で精確化)**: 本番APIの`/api/health`に`deploy.commit`が**含まれていない**(HTTP 200)。これはPR #99(deploy素性ブロック追加)**以前のコード**が稼働していることを意味する。本番チェックアウトを読み取り専用で確認した結果、**branch=`feat/native-dimension-hatch-viewport`、HEAD=`dfc32d9`、`origin/main`に対し4コミット先行・0コミット後退**であった。つまり本番にはPR #99〜#107(認証fail-closed、入力検証、CSP/HSTS、レート制限、ESLint、コマンドop検証、案件ACL fail-closed等)と、本ラウンドの修正(PR #108〜#111: ネイティブDXF往復12件、監査TRUNCATE保護、復元不能バックアップの修復)が**未反映**。distのタイムスタンプは2026-09-18 12:33で、DXF修正コミットより前。`scripts/deploy-local.sh`は`git merge --ff-only origin/main`で開始するため、分岐したままでは**安全に中止**する(自動ロールバックではなく開始時に停止)。解消には本番チェックアウトでの分岐の解消(暫定マージcommitとorigin/mainの統合方針の決定)が必要 |

## 3か月以内

| ID | 内容 | 対象/効果 | 難易度・工数 | 優先度 | 依存/リスク | 完了基準 |
| --- | --- | --- | --- | --- | --- | --- |
| P1-01 | 案件/組織/図面ACL | 全利用者/最小権限 | 高 15日(うちAPI/DB基盤2026-09-18完了、残13日) | P0 | Entra group、DB設計、管理UI(P1-09) | 横断IDOR test、監査、管理手順。**一部完了(2026-09-18、PR #95)**: `migrations/0007_project_membership.sql`で`projects.access_scope`(既定`open`、既存案件は無影響)と`project_members`を追加。`requireDrawingAccess`/`requireProjectAccess`をGET/POSTの全drawing系ルート(取得・transaction・comment・agent-run・approve・review)へ一貫適用し、restricted案件の非会員には図面不存在と同一の404を返しIDを推測されないようにした。`cad_admin`限定で`/projects`(作成)・`/projects/:id`(取得・accessScope変更)・`/projects/:id/members`(追加・削除)を新設。横断IDOR回帰テスト6件(`tests/project-access.test.js`)+実PostgreSQL統合テスト追加、`npm run verify`全成功、本番`mirai_web_cad`/`mirai_web_cad_mvp`両DBへ適用済み。**残タスク**: Entra IDグループに基づく案件メンバー自動同期(現状メール個別登録のみ)、案件作成・メンバー管理のUI(現状API直叩きのみ、P1-09と統合予定)、案件一覧・検索(P1-02) |
| P1-02 | 案件・図面一覧/検索 | 現場・本社/主要flow | 中 10日 | P1 | P1-01 | empty/error/page test、1万件検索 |
| P1-03 | DXF書出し実装 | CAD担当/正本互換 | 中 10-15日(+実案件コーパス収集待ち) | P0 | 実装着手自体に依存なし。ただし完了基準の「実案件100図面のDXF往復許容差合格」は[100図面台帳](compat-corpus/README.md)の実図面収集・利用許諾取得(P0-38、2026-08-30時点で台帳0件、人間側タスク)に依存する。実装完了(コード)と受入完了(実案件許容差合格)は別のマイルストーンとして扱う | 実装完了基準: DXF書出しが往復比較(`--mode=dxf-roundtrip`)で合格スコアを返す。受入完了基準: 実案件100図面のDXF往復許容差合格(実図面到着後)。**一部完了(2026-08-30)**: [ADR-0001](adr/ADR-0001-dwg-dxf-roundtrip-engine.md)(DWG対応選定)は[ADR-0002](adr/ADR-0002-dwg-scope-drop-dxf-only.md)により破棄。DWGを恒久的に対象外としDXF単体運用へ確定したため、ODAライセンス契約判断・DWG読込/書出しタスクは不要化し、SDK契約待ちの依存関係も解消。**一部完了(2026-09-04、P0-43)**: `src/dxf-export.js`(ASCII DXF R2000系、HEADER+LAYER table+ENTITIES。line→LINE/circle→CIRCLE/polyline→LWPOLYLINE/rect→閉鎖LWPOLYLINE/text→TEXT。dimension/hatch/blockは黙って捨てずskipped理由付きで報告、真色420/線種/線幅は後続Phase)とUI「DXF書出し」ボタン、`--mode=dxf-roundtrip`の実動化(exit 2スタブ撤廃)を実装。`tests/dxf-export.test.js`9件(往復一致・スキップ報告・座標丸め等)、`tests/compat-report.test.js`のdxf-roundtripテストを合格スコア検証へ更新。残タスク: 未対応entity(dimension/hatch/block)の書出し、真色・線種・レイアウト/図枠の保持、実案件での受入検証 |
| P1-04 | 寸法・公差・尺度 | CAD担当/日常作図 | 高 15日 | P1 | CAD kernel | linear/aligned/angular test、PDF一致 |
| P1-05 | hatch/block属性 | CAD担当/図面標準 | 高 20日 | P1 | DXF model | round-trip、編集、異常系 |
| P1-06 | layout/plot/PDF | CAD担当・承認者/成果物 | 高 20日 | P0 | font/plot style | A1-A4、尺度、線幅、font受入 |
| P1-07 | autosave/crash recovery | CAD担当/消失防止 | 高 12日 | P0 | IndexedDB暗号化 | 30秒以内復元、競合処理test |
| P1-08 | 版比較・差分承認 | 承認者/説明性 | 中 10日 | P1 | command event | add/update/delete可視化、署名監査 |
| P1-09 | 管理UI・利用者/role | IT/DX/少人数運用 | 中 10日 | P1 | P1-01 | group同期、棚卸export、異常系 |
| P1-10 | SBOM/License/Security/CODEOWNERS | CTO/供給網統制 | 低 3日 | P1 | 法務方針 | CI生成、責任者、脆弱性窓口 |
| P1-11 | オフサイトバックアップ・本番DB自動復旧ドリル | 全利用者/災害対策(DR) | 中 5日 | P0 | R2等の転送先契約、暗号化・保持期間方針の合意 | 日次バックアップがホスト外(Cloudflare R2等)へ暗号化転送され、本番DB(`mirai_web_cad`、MVPではない方)向けの自動復旧ドリルがMVPと同様にweekly timerで実行される。2026-09-18監査時点でMVP専用DBのみ自動復旧ドリルがあり、本番DBは手動実行前提(`docs/runbooks/database-incident.md`)。**一部完了(2026-09-18、PR #99)**: 本番DB用の週次復元ドリルunit(`deploy/systemd/mirai-web-cad-restore-drill.service`/`.timer`、日曜04:10 JST)と初回準備手順を追加。ただし実行には隔離DB`mirai_web_cad_recovery`の作成が必要で、**本番接続ロールにCREATEDB権限が無く`permission denied to create database`で失敗したため、DB管理者による初回準備待ち**。オフサイト転送・暗号化・保持期間の合意は未着手 |
| P1-12 | 障害当番表の実名・連絡先確定 | IT/DX 7名/インシデント対応の実効性 | 低 0.5日(人手の合意が前提) | P0 | IT/DX部門内の合意 | `docs/operations.md`の重大度別SLA・エスカレーション経路表がテンプレートのままでなく、実名・連絡先で確定している(Issue #8) |
| P1-13 | 単一ホスト依存の冗長化・DR先の検討 | 全利用者/事業継続性 | 高 20日(要経営判断) | P0 | 予算・マネージドDB/ホスティングの契約判断 | 本番(このホスト、kensan1969常駐)の電源・ネットワーク障害時にも一定時間内でサービス継続または迅速復旧できる代替経路(UPS、複数ホスト、クラウド回帰等)が合意・実装される。600名規模の本番正本として運用するための必須条件 |

## 6～12か月

| ID | 内容 | 対象/効果 | 難易度・工数 | 優先度 | 依存/リスク | 完了基準 |
| --- | --- | --- | --- | --- | --- | --- |
| P2-01 | 100k図形描画/空間index/Web Worker | CAD担当/性能 | 高 30日 | P0 | kernel | p95入力100ms、30fps、長時間test。**一部着手(2026-08-30)**: `drawCanvas`にviewportカリング(`boundsIntersect`/`entityBounds`、`src/cad-core.js`・`src/app.js`)を実装し、可視範囲外の図形描画を省略。描画コストのみ改善で、`hitTest`・`entities.find`等のO(n)検索(選択・ドラッグ・ID参照)は未対応。空間index(quadtree/rbush等)導入、差分描画、Web Worker化は未着手。残作業は大規模改修のため別途スコープ確定が必要 |
| P2-02 | PWA/offline閲覧・markup | 現場/通信断対応 | 高 25日 | P1 | 暗号・同期設計 | 8時間offline、再同期競合test |
| P2-03 | SharePoint/OneDrive連携 | 全社/M365正本連携 | 高 20日 | P1 | Graph API/retention | ACL継承、version、DLP確認 |
| P2-04 | 協力会社secure share | 協力会社/配布削減 | 高 15日 | P1 | B2B/期限 | 期限・透かし・download監査 |
| P2-05 | 電子納品命名/属性検査 | 公共工事/手戻り削減 | 中 15日 | P1 | 国交省最新要領 | 年版管理、一次資料link、受入test |
| P2-06 | 座標系/測地系/GIS layer | 土木技術者/位置整合 | 高 30日 | P1 | EPSG/国土地理院条件 | CRS明示、変換誤差、出典lineage |
| P2-07 | API/webhook/Excel export | DX/連携 | 中 15日 | P2 | ACL/API version | OpenAPI契約、rate limit、監査 |
| P2-08 | data quality dashboard | 管理者/品質監視 | 中 12日 | P1 | ルール定義 | 欠損・重複・揺れ・取得失敗alert |

## 将来

| ID | 内容 | 対象/効果 | 難易度 | 優先度 | 完了基準 |
| --- | --- | --- | --- | --- | --- |
| P3-01 | ACL付き仕様書/過去図面RAG | 技術者/検索短縮 | 高 | P2 | 出典・版・page・信頼度、権限test |
| P3-02 | 図枠/注記/属性の構造化抽出 | CAD担当/転記削減 | 高 | P2 | 精度指標、human correction、監査 |
| P3-03 | 数量・尺度・座標異常候補 | 承認者/見落とし低減 | 高 | P2 | 再現率/誤検知、責任分界、停止手段 |
| P3-04 | BIM/IFC参照と2D切出し | 技術者/連携 | 高 | P3 | IFC版、座標、属性保持test |
| P3-05 | Sheet Set/外部参照 | CAD担当/大規模案件 | 高 | P2 | path解決、版固定、offline test |

## 追加機能分類（25件）

| 分類 | 機能 | 対象 | 効果 | 難易度 | 優先度 | 差別化 |
| --- | --- | --- | --- | --- | --- | --- |
| 業務 | 案件/工区/図面台帳 | 全社 | 正本所在統一 | 中 | P0 | 土木案件階層 |
| 業務 | レビュー差戻し/comment | 承認者 | 手戻り明確化 | 中 | P1 | 社内決裁統合 |
| 業務 | 図面配布/受領確認 | 現場/協力会社 | 配布証跡 | 中 | P1 | 工区別配布 |
| 地図 | CRS/測地系変換 | 土木技術者 | 座標事故防止 | 高 | P1 | 日本測地系運用 |
| 地図 | 国土地理院layer | 現場 | 現況把握 | 中 | P2 | 一次情報lineage |
| 検索 | 図面/属性/全文検索 | 全社 | 探索時間削減 | 中 | P1 | 案件ACL検索 |
| 可視化 | 版差分overlay | 承認者 | 変更把握 | 中 | P1 | 承認証跡連動 |
| Mobile/PWA | 現場markup | 現場 | 帰社転記削減 | 高 | P1 | 写真/位置/図面連動 |
| Offline | 暗号化offline cache | 現場 | 通信断対応 | 高 | P0 | 現場同期policy |
| 通知 | review/期限通知 | 承認者 | 滞留削減 | 低 | P1 | Teams/Exchange連携 |
| PDF/Excel | 尺度付きPDF plot | CAD担当 | 成果物作成 | 高 | P0 | 発注者図枠preset |
| PDF/Excel | 数量Excel export | 積算/現場 | 転記削減 | 中 | P1 | 工種別template |
| API | OpenAPI + webhook | DX | 自動連携 | 中 | P2 | M365/台帳接続 |
| RBAC/監査 | 案件ACL | 管理者 | 権限逸脱防止 | 高 | P0 | Entra group同期 |
| RBAC/監査 | 監査export/保全 | 監査者 | 説明責任 | 中 | P0 | 保存年限policy |
| 管理運用 | 利用者/role棚卸 | IT/DX | 7名運用 | 中 | P1 | 差分承認 |
| 管理運用 | SLO/alert dashboard | IT/DX | 障害短縮 | 中 | P0 | 主要flow監視 |
| データ品質 | 欠損/重複/表記揺れ検査 | 管理者 | 品質維持 | 中 | P1 | 自治体別rule |
| データ品質 | 取得lineage/利用条件 | 管理者 | 出典説明 | 中 | P1 | 一次情報優先 |
| 土木固有 | 電子納品check | 公共工事担当 | 不備削減 | 高 | P1 | 要領年版管理 |
| 土木固有 | 土工数量/測点 | 技術者 | 計算時間削減 | 高 | P2 | 断面・測点連携 |
| 土木固有 | 図枠/工種template | CAD担当 | 標準化 | 中 | P1 | 社内標準preset |
| AI | ACL付きRAG | 技術者 | 検索・判断支援 | 高 | P2 | 根拠引用 |
| AI | 図面属性抽出 | CAD担当 | 転記削減 | 高 | P2 | human correction学習 |
| AI | 異常候補検出 | 承認者 | 見落とし低減 | 高 | P2 | rule+model説明 |
